import { describe, it, expect } from 'vitest';

describe('@geotrack/core', () => {
  it('package infrastructure is set up', () => {
    // Placeholder — real tests added with each feature task
    expect(1 + 1).toBe(2);
  });
});
