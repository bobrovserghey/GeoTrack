# GeoTrack — технический план разработки MVP

Дата: 2026-09-11, обновлено 2026-09-25. Версия 1.7. Проект: GEO. Автор: Serghey (ProDuck:t Space) при участии Claude. Связанные документы: GEOTRACK_PRODUCT_CONCEPT_RU.md (v1.3), GEOTRACK_AUDIT_METHODOLOGY_RU.md, GEOTRACK_MVP_AND_ARCHITECTURE_RU.md (v1.1), GEOTRACK_ROADMAP_RU.md, GEOTRACK_DESIGN_BRIEF_RU.md, GEOTRACK_TECH_PLAN_CTO_REVIEW_RU.md, GEOTRACK_TECH_PLAN_DECISIONS_RU.md, GEOTRACK_UNIT_ECONOMICS_CHECK_RU.md.

## Резюме

Документ переводит архитектуру и роудмэп MVP в исполняемый план разработки: порядок написания кода и тестов, контракты между блоками, контур безопасности и операционную модель работы связки «продакт плюс Claude Code». Он отвечает не на вопрос «что строим» — это закрыто концепцией и методологией, — а на вопрос «как строим, в каком порядке и как не сломать то, что уже работает». Версия 1.1 включает правки по итогам ревью с позиции CTO: профили аудита, жизненный цикл аудита, канонические промпты категорий, правила доставки платного отчёта при сбоях, защиту исходящих запросов воркера, опрос ленты прогресса и уточнённую себестоимость. Версия 1.2 добавляет методику промптов: локали «язык плюс рынок» с промптами на языке локали, персоны запроса, клиентский слой промптов и прогон без веб-поиска (задачи T-59–T-68). Версия 1.3 добавляет профиль аутрича с общим прогоном категории на пачку и извлечение фактов из категорийного ответа один раз для всех аудитов (задачи T-69–T-74). Версия 1.4 добавляет бюджеты аутрича, которые продакт задаёт в админке: на компанию, на пачку с числом компаний и на месяц (раздел 4.10, задачи T-75 и T-76). Версия 1.5 делает аутрич и лендинг одной точкой входа в воронку: адресат письма получает тот же Free snapshot, подготовленный заранее.

**Ключевые предположения**:
- Разработчик — LLM-агент, человеческого код-ревью в привычном объёме не будет. Значит, качество переносится в контракты, тесты и автоматические гейты, а не в чтение кода.
- Половина системы недетерминирована: ответы движков и извлечение фактов моделью не воспроизводятся дословно. Для этой половины классические тесты не работают, нужны золотые наборы и пороги точности.
- Сроки фазы 1 фиксированы гейтами G1, G2 и G3; при отставании двигается скоуп, а не даты.

**Рекомендация**: начать с недельного «скелета» — пустой аудит, проходящий насквозь через все слои на заглушках, — и дальше вести вертикальные срезы по столпам, доводя каждый до баллов, находок и отображения в отчёте, прежде чем начинать следующий.

**Статус**: готово к использованию как рабочий документ фазы 1 — версия 1.7; объём до гейта G1 не меняется, новые задачи идут в недели 5–9.

---

## 1. Назначение, границы и место среди документов

### 1.1 Что решает этот документ

Шесть существующих документов отвечают на вопросы «зачем», «кому», «что измеряем», «из чего состоит система», «как это выглядит» и «как продаём». Между ними и первым коммитом остаётся разрыв: непонятно, в каком порядке писать код, какими тестами закрывать вероятностные части, как устроена безопасность публичного сервиса, который тратит деньги на каждый запрос, и как организовать цикл разработки, где код пишет агент, а ревьюер один и он же продакт. Этот документ закрывает разрыв.

Он предназначен для двух читателей. Первый — продакт, который ставит задачи и принимает результат. Второй — Claude Code, для которого этот документ вместе с CLAUDE.md является источником правил проекта.

### 1.2 Границы

В документе: фаза 1, десять недель, с 7 сентября по 15 ноября 2026 года, до гейта G3. Фазы 2 и 3 упоминаются только там, где решение недели 1 нельзя принять, не заглянув вперёд: выбор оркестратора, схема данных, разделение хранилищ.

Вне документа: продуктовые решения (концепция), состав критериев и веса (методология), визуальный дизайн (дизайн-бриф), план продаж (стратегия продаж).

### 1.3 Приоритет источников истины

Конфликты между документами неизбежны, поэтому порядок старшинства задаётся один раз:

| Вопрос | Источник истины |
|---|---|
| Что измеряем, какие критерии и веса | GEOTRACK_AUDIT_METHODOLOGY_RU.md |
| Что входит в скоуп, какой стек | GEOTRACK_MVP_AND_ARCHITECTURE_RU.md |
| Сроки, гейты, критерии перехода | GEOTRACK_ROADMAP_RU.md |
| Экраны, компоненты, токены | GEOTRACK_DESIGN_BRIEF_RU.md |
| Состав тарифов | GEOTRACK_PRODUCT_CONCEPT_RU.md, раздел 6.6 |
| Порядок работ, тесты, безопасность, правила разработки, профили и жизненный цикл аудита | этот документ |
| Действующие соглашения кодовой базы | CLAUDE.md в репозитории |

Расхождение между документом и кодом означает ошибку в документе или незафиксированное решение. Оба случая закрываются записью в `docs/adr/`, а не молчаливым расхождением.

---

## 2. Исходные условия и инженерные принципы

### 2.1 Условия, которые нельзя изменить

Один человек с 20–25 часами в неделю на продукт. Один разработчик-агент, работающий сессиями без памяти между ними. Бюджет инфраструктуры и API 400–800 долларов в месяц. Три гейта с фиксированными датами. Продакшен-инцидент некому разбирать ночью. Наёмного инженера в фазе 1 нет.

Из этих условий следует всё остальное. Система должна быть скучной в эксплуатации: управляемые сервисы, никакого Kubernetes, минимум движущихся частей, деградация вместо падения, а стоимость ошибки должна быть ограничена архитектурно, а не дисциплиной.

### 2.2 Девять инженерных принципов

1. **Контракт раньше реализации.** Любой блок начинается с типов входа и выхода в `packages/core`. Реализация может быть заглушкой, контракт — нет.
2. **Модель извлекает факты, баллы считает код.** Ни один балл, ни одно правило блокера, ни один приоритет находки не определяются моделью. Модель превращает текст в структуру, дальше работает детерминированный код. Это одновременно требование воспроизводимости скора, обещанное пользователю в концепции, и главная защита от инъекций из чужого контента.
3. **Каждый шаг идемпотентен.** Ключ шага — `audit_id`, имя шага и версия. Повторный запуск не дублирует записи и не тратит деньги повторно без явного указания.
4. **Деньги считаются в момент траты.** Каждый вызов провайдера записывает токены и стоимость в контексте аудита. Аудит, превысивший потолок своего профиля, останавливается с пометкой, а не доходит до счёта в конце месяца.
5. **Внешний мир — за адаптером.** Движки, поисковые API, платежи, письма доступны коду только через интерфейс из `core`. Замена провайдера — это конфигурация и новый адаптер, а не правка пайплайна.
6. **Тесты не ходят в сеть.** Любой сетевой вызов в тестах запрещён на уровне окружения. Внешнее поведение воспроизводится фикстурами. Живые вызовы — отдельный ночной прогон.
7. **Деградация вместо падения.** Недоступность одного движка или одного внешнего API помечает столп как частичный и понижает доверие к скору, но не роняет аудит.
8. **Небольшая задача, зелёный CI, один PR.** Задача размером два-три часа работы агента. Слияние только при зелёном CI. Автоматического слияния нет.
9. **Решение живёт в репозитории.** Архитектурное решение фиксируется в `docs/adr/` одной страницей. Того, чего нет в репозитории, для следующей сессии агента не существует.

### 2.3 Архитектурные инварианты

Инвариант — то, что агент не меняет по собственной инициативе, даже если так проще пройти тест. Изменение инварианта требует записи ADR и явного согласия продакта. Список короткий и висит в CLAUDE.md:

- контракт шага пайплайна и правило идемпотентности;
- таблица переходов статусов аудита (раздел 4.8);
- файл профилей аудита (раздел 4.7);
- интерфейсы адаптеров движка и модели;
- схема базы данных и миграции;
- файл конфигурации весов, порогов, формулы интервала и правил блокеров;
- граница пакетов: `core` не зависит ни от `web`, ни от `worker`;
- запрет на удаление и отключение существующих тестов;
- модель доступа к отчёту и админке;
- модуль исходящих запросов воркера и фабрика контекстов Playwright (раздел 9.2);
- справочник локалей и правило «промпты генерируются на языке локали, а не переводятся» (раздел 7.3);
- список внешних провайдеров и ключей;
- жёсткие пределы бюджета аутрича в `config` (раздел 4.10) — рабочие значения ниже них продакт меняет в админке без ADR.

---

## 3. Репозиторий, среда и правила изменений

### 3.1 Структура монорепозитория

```
geotrack/
  apps/
    web/                Next.js 15: лендинг, отчёт, админка, API-роуты
    worker/             Node 22: функции Inngest, Playwright, краулер
  packages/
    core/               схемы, контракты, скоринг, адаптеры, учёт стоимости, safe-fetch, переходы статусов
    config/             веса, пороги, таксономия и персоны запроса, шаблоны находок, профили аудита, справочник локалей, промпты
      prompts/
        categories/     канонические наборы промптов по категориям и локалям
        tasks/          промпты задач модели по локалям
    db/                 Drizzle-схема, миграции, сиды
    ui/                 общие компоненты отчёта для веба и PDF
  docs/
    specs/              спецификации задач, debt.md
    adr/                архитектурные решения
    runbooks/           рантбуки
    product/            документы проекта
  fixtures/
    sites/              HTML-снимки эталонных сайтов
    engines/            записанные ответы движков
    golden/             размеченные наборы для eval
  CLAUDE.md
```

Отдельный пакет `config` нужен по причине из архитектурного документа: продакт правит веса, таксономию, шаблоны находок, профили и промпты сам, без цикла разработки. Держать их внутри `core` значит смешивать код и данные и заставлять агента трогать файлы, которые он трогать не должен.

### 3.2 Правила зависимостей

`core` не импортирует ничего из `apps`. `web` и `worker` не импортируют друг друга. Общий код отчёта живёт в `ui` и рендерится и в браузере, и в Playwright при сборке PDF — это единственный способ гарантировать, что PDF и веб-отчёт не разойдутся. Нарушение правила зависимостей ловится линтером, а не ревью. Тем же линтером ловится импорт `undici` или `playwright` мимо модулей из раздела 9.2.

### 3.3 Локальная среда

Цель — полный прогон аудита на фикстурах без единого внешнего вызова и без ключей. Достигается тремя вещами: docker-compose с Postgres и Redis, режим `PROVIDERS=fixture`, при котором все адаптеры читают из `fixtures/`, и сид-набор из трёх сайтов с заранее записанными ответами. Новый разработчик-сессия должна получать зелёные тесты после `pnpm install` и `pnpm test`, не имея ни одного секрета.

### 3.4 Ветки, коммиты, PR, миграции

Ветка на задачу, имя по идентификатору спецификации: `t-13-competitors`. Коммиты произвольные, важно описание PR по шаблону из приложения Ж. Слияние в `main` только через PR с зелёным CI. `main` деплоится автоматически в прод, каждый PR — в preview-окружение.

Миграции только вперёд, файлом в `packages/db/migrations`, с понятным именем. Миграция, удаляющая колонку или таблицу, требует отдельного PR и явного согласия продакта: агент не имеет права удалять данные попутно.

### 3.5 Конфигурация и секреты

Ключи не попадают в репозиторий никогда. В репозитории живёт `.env.example` с перечнем переменных и комментариями. Ключи разделены по окружениям: у dev-окружения собственный проект Supabase и собственные ключи провайдеров с низким лимитом расходов, выставленным на стороне провайдера. Продакшен-ключи не существуют в среде, где работает агент.

### 3.6 Контур веб-приложения

Поверхность веб-приложения фиксируется заранее, чтобы задачи интерфейса и задачи пайплайна не пересекались в одних файлах.

| Тип | Адреса | Кто открывает |
|---|---|---|
| Страницы | Лендинг, прогресс аудита, результат тизера, отчёт по токену, оплата и её результат, методология, юридические | Аноним, кроме админки |
| Админка | Список аудитов, карточка аудита, артефакты, очередь отчётов на проверку, метрики | Администратор по роли |
| API аудита | Создание аудита, подтверждение категории, ввод email, опрос ленты событий, запрос PDF | Аноним с ограничением частоты |
| API служебный | Вебхук платежей, вебхук почты, перезапуск шага, возврат платежа, удаление данных, внутренний рендер отчёта для PDF | Провайдер по подписи, админ по роли, воркер по сервисному секрету |

Локализация покрывает страницы, отчёт и письма; админка остаётся на одном языке. Язык интерфейса и отчёта (en, ro) и локаль промптов (en, ro-MD, ru-MD, ro-RO) — разные настройки: русский существует только как локаль промптов. Страницы отчёта, прогресса и результата отдают заголовки запрета индексации, лендинг и методология индексируются.

---

## 4. Контракты: на чём держится код

### 4.1 Schema-first

Единый источник истины для типов — Zod-схемы в `core`, из которых выводятся типы TypeScript, и Drizzle-схема в `db`, из которой выводятся типы таблиц. Совпадение двух наборов проверяется тестом: для каждой сущности, которая и сохраняется, и валидируется, есть проверка, что объект, прошедший Zod, укладывается в тип вставки Drizzle. Это дешёвый тест, который ловит целый класс ошибок агента при изменении схемы.

### 4.2 Контракт шага пайплайна

Каждый из пятнадцати шагов — функция одной формы:

```ts
type StepResult<T> = {
  status: 'ok' | 'partial' | 'failed'
  data: T | null           // при partial — заполнен тем, что удалось собрать
  artifacts: ArtifactRef[]   // ссылки на HTML-снимки, скриншоты, сырые ответы
  usage: UsageRecord[]       // провайдер, модель, токены, стоимость
  notes: string[]            // что пропущено и почему — попадает в отчёт
}

type Step<I, O> = (input: I, ctx: StepContext) => Promise<StepResult<O>>
```

`StepContext` даёт шагу доступ к логгеру, идемпотентному ключу, профилю аудита, бюджету оставшейся стоимости и функции записи события в ленту прогресса. Шаг не создаёт соединений и не читает переменные окружения сам — всё приходит через контекст. Шаг не проверяет тип аудита условиями: объём работы задаёт профиль (раздел 4.7). Это делает шаг тестируемым без инфраструктуры.

Статус `partial` — полноправный, а не признак ошибки. Столп, собранный частично, даёт баллы по измеренным критериям, помечает неизмеренные и расширяет доверительный интервал.

### 4.3 Контракт адаптера движка

```ts
interface EngineAdapter {
  id: EngineId              // строковый идентификатор из реестра в config
  ask(prompt: string, opts: AskOptions): Promise<EngineAnswer>
}

type EngineAnswer = {
  text: string
  sources: { url: string; title?: string }[]
  raw: unknown          // сырой ответ, сохраняется в артефакты
  usage: UsageRecord
}
```

Реестр движков — файл в `config` с идентификатором, провайдером, моделью, лимитом конкурентности и возможностями адаптера: поддержка режима без веб-поиска и передачи региона пользователя. `AskOptions` передаёт режим (`search: true | false`), локаль и страну рынка. Если движок не принимает регион, адаптер пишет это в `notes`, и отчёт описывает метод честно. Состав реестра на старт — четыре движка: Perplexity, ChatGPT, Gemini и Claude (концепция, 9.2, пункт 6). Claude опрашивается только в платных профилях и только в режиме с поиском. Meta AI и его агент Muse в реестр не входят, решение о Meta — на старте фазы 2. Добавление любого движка — изменение списка внешних провайдеров, то есть инварианта, через ADR. Новый движок фазы 2 — запись в реестре и адаптер, без правки типов. Требования к любому адаптеру одинаковы: повтор при 429 с экспоненциальной задержкой, потолок попыток, таймаут, нормализация источников (развёртывание редиректов, приведение к домену), сохранение сырого ответа. Эти требования проверяются одним общим набором контрактных тестов, который прогоняется для каждого адаптера. Новый движок считается готовым, когда проходит общий набор, — отдельных тестов под него писать не нужно.

### 4.4 Контракт модели и структурированный вывод

Слой извлечения работает только в режиме «текст на входе, JSON по схеме на выходе». Правила: схема Zod обязательна; поля максимально ограничены перечислениями, а не свободным текстом; при ошибке валидации один повтор с текстом ошибки; при второй ошибке — запасной провайдер; при третьей — шаг возвращает `partial`. Модель не получает инструментов, не ходит в сеть и не пишет в базу.

### 4.5 Версионирование методологии

В `config` лежит файл `methodology.v1.json`: веса столпов, баллы критериев, пороги полос, правила блокеров с признаком применимости, формула доверительного интервала и её константы, правило обращения с неизмеренными критериями. Каждый аудит сохраняет `methodology_version`, `prompt_set_version` и `profile_version`. Скоринг — чистая функция от структурированных фактов и версии конфигурации. Из этого следуют три полезных свойства: любой старый аудит можно пересчитать новой версией весов и сравнить; калибровка на неделе 9 не требует правки кода; изменение веса — это изменение данных, которое делает продакт.

Три формулы, которых нет в методологии и которые фиксируются здесь, чтобы тесты можно было написать до кода:

- **Неизмеренный критерий** исключается из знаменателя столпа: балл столпа — сумма баллов измеренных критериев, делённая на сумму их максимумов, умноженная на 100. Правило методологии о перераспределении E6 на E1 и E4 не применяется, это единое правило для всех столпов.
- **Доверительный интервал** — полуширина `h = h_rep + h_cov`. `h_rep` — разброс столпа A между повторами, переведённый в итоговый скор: `0,30 × σ_A / 10`; при одном повторе `h_rep` берётся константой из конфигурации (стартовое значение 0,3, уточняется калибровкой). `h_cov = 10 × 0,5 × w_unm`, где `w_unm` — доля веса неизмеренных критериев в итоговом скоре. Интервал обрезается границами шкалы.
- **Прогноз корзины** — текущий скор плюс сумма по выбранным услугам: потерянные баллы критериев, которые услуга закрывает, умноженные на вес и на коэффициент реализации из шаблона услуги (стартовое значение 0,7). Приоритет находки — отношение потерянных баллов в итоговом скоре к часам усилий из шаблона.

**Правило для платного клиента**: отчёт показывается в версии методологии, действовавшей на момент оплаты. Пересчёт новой версией доступен как отдельный вид «в текущей методологии» с явной пометкой, скор в письме и PDF не меняется.

**Индикатор «знание без поиска»** — доля промптов прогона без поиска, где бренд назван. В методологии v1 он не входит в скор: показывается рядом со столпом A и управляет выбором находок (раздел 7.4). Включение в скор — решение калибровки и новая версия методологии.

**Скор при нескольких локалях.** Итоговый скор считается по основной локали. Каждая дополнительная локаль расширенного профиля даёт отдельный балл столпа A по тем же критериям A1–A6, в общий скор он не входит.

### 4.6 Схема данных v1

Состав таблиц взят из архитектурного документа. Здесь фиксируются правила обращения с ней и поля, добавленные в версии 1.1:

- Сырые артефакты (HTML, полные ответы движков, скриншоты) живут в Storage, в базе — только ссылка, размер и хэш. База не должна пухнуть от текстов.
- Все денормализованные числа скора выводятся из `scores`, а не пересчитываются в трёх местах.
- `audit_events` — только лента прогресса, не источник состояния. Каждое событие имеет монотонный `seq` в рамках аудита. Состояние аудита — поле `audits.status` по таблице переходов раздела 4.8.
- Персональные данные в MVP — это email и домен. Удаление по запросу реализуется одной функцией, написанной сразу, а не «потом».
- Для каждой таблицы с доступом снаружи включён RLS с политикой по умолчанию «запрещено».

Таблицы группируются по сроку жизни и по тому, кто их пишет. Это определяет и порядок миграций, и будущее разделение хранилищ в фазе 2.

| Группа | Таблицы | Кто пишет и зачем |
|---|---|---|
| Аудит и ход работы | `audits`, `audit_events`, `users`, `orders`, `payments` | Веб и оркестратор. Ключевые поля `audits`: `status`, `profile_id`, `profile_version`, `parent_audit_id`, `is_internal`, `prompt_locale`, `extra_locales`, `report_lang`, `buyer_questions`, `methodology_version`, `prompt_set_version`, стоимость. `orders`: `product_id`, `parent_audit_id`, идентификатор события Paddle |
| Сайт и продукт | `sites`, `pages`, `passports`, `categories`, `competitors` | Воркер, шаги 1–2. Снимки страниц и метрики, паспорт продукта, категория с уверенностью и признаком автовыбора |
| Измерения | `prompts`, `engine_runs`, `mentions`, `tech_checks`, `offsite_signals`, `agent_runs` | Воркер, шаги 3–9. Самая объёмная группа, в фазе 2 выгружается во временные ряды |
| Результат | `scores`, `findings` | Шаги 10–11. Баллы по критериям и столпам, находки семи полей |
| Служебное | `category_cache` | Шаг 6. Ключ — идентификаторы (раздел 8.4), значение — список ответов по повторам |

Два поля есть в каждой таблице измерений: `audit_id` и версия шага. Они дают идемпотентность, возможность перезапустить один шаг из админки и возможность пересчитать старый аудит. Флаг `is_internal` исключает аудит из бенчмарков, воронки, счётчиков гейтов и лимитов по домену; он ставится на ночные прогоны, калибровку и preview-аудиты.

В `prompts` и `engine_runs` версия 1.2 добавляет поля: класс промпта (`category`, `brand`, `client`), `persona_id`, режим (`search`, `no_search`), локаль и источник для клиентских промптов. В `passports` — варианты написания бренда и целевая персона запроса. Поля добавляются новой миграцией, существующие не переписываются.

### 4.7 Профили аудита

Тизер и два тарифа — три набора параметров пайплайна. Они лежат в `packages/config/audit-profiles.v2.json`; аудит хранит `profile_id` и `profile_version`; шаги читают параметры из контекста. Состав тарифов взят из концепции продукта (раздел 6.6, версия 1.3): стандартный — четыре движка, 30 промптов с поиском и 10 без поиска на одной локали, три конкурента; расширенный — 50 промптов, до трёх локалей, пять конкурентов, созвон 15 минут. Версия профилей v1 остаётся в репозитории для пересчёта старых аудитов.

| Параметр | Тизер | Стандартный $79 | Расширенный $199 |
|---|---|---|---|
| Страниц краулера | 3 | 50 | 50 |
| Движки | Perplexity | Perplexity, ChatGPT, Gemini, Claude | Те же четыре |
| Локали промптов | 1 | 1 | Основная и до двух дополнительных |
| Промпты с поиском, основная локаль | 12: 10 категорийных, 2 брендовых | 30: 20 категорийных, 4 брендовых, 6 клиентских | 50: 30 категорийных, 10 брендовых, 10 клиентских |
| Промпты с поиском, дополнительная локаль | — | — | 25: 20 категорийных, 5 брендовых |
| Прогон без поиска | Нет | 10: 6 discovery, 4 брендовых; ChatGPT | 20: 10 discovery, 10 брендовых; ChatGPT и Gemini |
| Повторы | Один | Два | Два |
| Конкуренты | 0, только упоминания | 3 | 5 |
| Столпы | A предварительно, B, E5 | Все пять | Все пять |

Остальные параметры профиля: число находок (3 шаблонные, 15, 15); ручная проверка (нет, да, да); PDF (нет, да, да); запросов SerpAPI для B4 (2, 11, 11) и для клиентского слоя (0, 2, 2); мягкий и жёсткий потолок стоимости ($0,10 и $0,25; $6 и $8; $9 и $12 плюс $4 и $5 за каждую дополнительную локаль); бюджет машинного времени (90 секунд, 6 минут, 8 минут плюс 2 минуты за каждую дополнительную локаль). Созвон в расширенном тарифе — ссылка Cal.com в письме, не часть пайплайна.

**Переход из тизера в платный аудит.** Платный аудит — новый `audit_id` с полем `parent_audit_id`. Оплата возможна только со страницы результата тизера; соответствие `product_id` и профиля задаётся на сервере. Из тизера переиспользуются паспорт и подтверждённая категория, если тизеру меньше 14 дней; краулер выполняется заново всегда. Кэш категорий работает независимо от родства аудитов. Лимит «один тизер на домен в 30 дней» на платные аудиты не распространяется. Локаль промптов наследуется из тизера; дополнительные локали расширенного тарифа выбираются на странице выбора тарифа аудита перед оплатой, там же — до пяти вопросов покупателей для клиентского слоя. Оба значения сохраняются в тизере и читаются платным аудитом через `parent_audit_id`.

**Профиль аутрича.** Аутрич дарит адресату тот же Free snapshot, что получает любой посетитель лендинга: тот же объём, тот же экран результата, тот же email-гейт и тот же путь к оплате. Это одна точка входа в воронку для обоих каналов. Отличий два: снапшот готовится заранее, до письма, и домены собираются в пачки — от 20 до 40 доменов одной категории и одного рынка, — чтобы категорийные ответы были общими.

| Этап | Когда | Состав | Потолок стоимости |
|---|---|---|---|
| Прогон категории | Один раз на пачку, при запуске | 10 категорийных промптов профиля тизера, Perplexity, один повтор; извлечение один раз (раздел 8.4) | $0,10 и $0,25 на пачку |
| Снапшот домена | Для каждого домена, при запуске | Профиль тизера целиком: три страницы, паспорт, технический чек с двумя запросами SerpAPI, два брендовых промпта | Потолки тизера: $0,10 и $0,25 |

Брендовые промпты выполняются сразу, хотя во входящем потоке они ждут email. Адресат вводит email так же, как все, но результат к этому моменту уже готов и открывается без ожидания. Экономить на этом нет смысла: около двух центов на домен, зато нет отложенного запуска и риска, что бот предпросмотра ссылки запустит оплачиваемый прогон.

Аудит аутрича не помечается `is_internal`: это такой же вход в воронку, как лендинг. Он получает `source = outreach` и считается в метриках воронки отдельной строкой. Ручные тестовые прогоны из админки по-прежнему помечаются `is_internal`. Лимит «один тизер на домен в 30 дней» действует и здесь: если адресат потом сам вводит свой домен на лендинге, он попадает на уже готовый результат. Если на домен уже есть свежий снапшот из входящего потока, пачка не делает второй и берёт его цифры.

### 4.8 Жизненный цикл аудита

Состояние живёт в `audits.status`. Переходы выполняет одна чистая функция `transition(status, event)` в `core` с табличным описанием разрешённых переходов; она входит в детерминированное ядро, тесты пишутся до кода. Оркестратор и API вызывают только её.

| Статус | Что происходит | Выход из статуса |
|---|---|---|
| `queued` | Запись создана, событие отправлено | Оркестратор принял — `running` |
| `running` | Идут шаги | Событие ожидания, завершение, ошибка |
| `waiting_category` | Показано окно выбора | Выбор пользователя; таймаут 10 минут — автовыбор кандидата с наибольшей уверенностью, пометка в отчёте |
| `waiting_email` | Только тизер. Предварительный результат показан, брендовые промпты ждут email | Ввод email — `running`; таймаут 7 дней — `completed` с пометкой «брендовые промпты не выполнялись» |
| `completed` | Все шаги завершены. Для тизера терминальный | Платный — `in_review` |
| `in_review` | Только платный. Ждёт ручной проверки | Одобрение — `delivered`; окно 24 часа по разделу 8.3 |
| `delivered` | Токен отчёта активен, письмо отправлено. Терминальный | Перезапуск шага из админки не меняет статус |
| `needs_attention` | Сбой после траты денег, зависание, жёсткий потолок платного аудита | Действие админа: перезапуск, доставка частичного, возврат |
| `failed` | Терминальный. Тизер после сбоя или жёсткого потолка; платный после возврата | — |
| `cancelled` | Терминальный, только админ | — |

Правила:

- Зависание: `running` без событий 15 минут — платный аудит переходит в `needs_attention` и поднимает алерт, тизер через 20 минут — `failed` с сообщением пользователю.
- Ожидания реализуются через `step.waitForEvent` оркестратора с таймаутом. Повторное подтверждение категории или повторный email игнорируются, если аудит не в соответствующем статусе ожидания.
- Выбор категории после автовыбора не принимается: интерфейс показывает, с какой категорией продолжен аудит, и предлагает повторный аудит.
- Результат тизера виден с `waiting_email`, отчёт платного аудита — только с `delivered`.
- Экран прогресса рисует состояние из статуса, а не выводит его из последнего события.
- Снапшот аутрича проходит без ожидания email: брендовые промпты выполняются сразу, аудит завершается в `completed`, а экран результата показывает полную версию только после ввода email — как во входящем потоке. Открытие ссылки ничего не запускает и ничего не стоит.

### 4.9 Хранение данных, регион и сроки

Решения приняты 11 сентября 2026 года и заменяют умолчания архитектурного документа в части региона и сроков хранения.

**Регион — Европа.** Все хранилища создаются в европейском регионе Supabase с первого дня. Сейчас это стоит ноль, а перенос на фазе 3, когда появятся DPA и продажи mid-market, обойдётся в недели работы и простой сервиса.

**Три типа данных лежат в трёх разных местах**, и путать их нельзя:

| Тип данных | Где хранится | Срок | Доступ |
|---|---|---|---|
| Сырьё: HTML-снимки, полные ответы движков, скриншоты | Supabase Storage, приватный бакет | Тизер 30 дней, платный аудит 12 месяцев | Подписанная ссылка, только воркер и админка |
| Структурированные факты и баллы | Postgres | 12 месяцев | Сервер по токену отчёта, админ |
| Производные файлы: PDF отчёта | Supabase Storage | Как у платного аудита | Подписанная ссылка из отчёта |
| Кэш ответов по категориям | Postgres, таблица `category_cache` | 14 дней на запись, агрегаты бессрочно | Только воркер |

Сроки исполняются ежедневной задачей очистки (T-56), а не вручную. Разделение сроков по типу аудита осмысленно арифметически: тизеров за фазы 1 и 2 будет около 1 500 и почти ни один из них никто не откроет повторно, тогда как артефакты платного аудита нужны и для разбора жалоб, и для пересчёта новой версией весов.

**Условия провайдеров.** До задач T-27 и T-28 проверяются условия OpenAI, Perplexity и Google на хранение ответов и построение агрегатов: у Gemini grounding есть ограничения на хранение grounded-результатов. Результат проверки — ADR-017; если условия запрещают хранение сырья 12 месяцев, срок для соответствующего движка сокращается, агрегаты строятся только из извлечённых фактов.

**Готовый отчёт как файл не хранится.** Страница собирается из данных по токену при каждом открытии. Это даёт два бесплатных свойства: перезапуск шага после доставки дополняет отчёт без отдельной функции, а старый отчёт можно показать в новой версии методологии по правилу раздела 4.5.

**Объём и переход на другое хранилище.** Оценка: платный аудит даёт 2–10 мегабайт артефактов, тизер около половины мегабайта. За фазы 1 и 2 это единицы гигабайт, и Supabase Storage хватает с запасом. Cloudflare R2 остаётся вопросом фазы 3 и в MVP не рассматривается.

**Бэкапы.** Ежедневный автоматический бэкап базы. Восстановление проверяется один раз вручную до гейта G3 — непроверенный бэкап бэкапом не является.

### 4.10 Бюджеты аутрича

Аутрич тратит деньги до того, как адресат что-то сделал, поэтому на него есть отдельный бюджет, который продакт задаёт в админке. Все деньги пачки тратятся при запуске, поэтому бюджет проверяется до запуска и страхуется потолками во время прогона.

**Три уровня бюджета.**

| Уровень | Что ограничивает | По умолчанию | Жёсткий предел в `config` |
|---|---|---|---|
| Домен | Снапшот одного домена, без доли прогона категории | $0,10 | $0,25 |
| Пачка | Сумма в долларах и максимум доменов; прогон категории входит в сумму | $5 и 40 доменов | $20 и 100 доменов |
| Месяц | Все пачки аутрича за календарный месяц | $20 | $100 |

Значения по умолчанию взяты из расчёта себестоимости (GEOTRACK_UNIT_ECONOMICS_CHECK_RU.md, раздел 6): снапшот в пачке стоит $0,06–0,08, пачка из 40 доменов — $2,5–3, две пачки в месяц — около $6. Жёсткий предел домена совпадает с жёстким потолком профиля тизера.

Рабочие значения живут в Postgres, в таблице `outreach_settings` и в полях `outreach_batches`, а не в файле профилей: они меняются по ходу аутрича и не должны требовать цикла разработки. Жёсткие пределы лежат в `config` и являются инвариантом: они защищают от опечатки вроде $50 вместо $5. Значение выше предела админка не сохраняет.

**Как задаётся в админке.** Раздел «Аутрич, бюджет» хранит значения по умолчанию для домена и пачки и месячный лимит. При создании пачки продакт задаёт её сумму и число доменов; поля заполнены значениями по умолчанию. Каждое изменение пишется в журнал действий админки: кто, когда, старое и новое значение.

**Оценка до запуска.** Админка показывает оценку пачки: прогон категории (ноль, если кэш свежий) плюс число доменов на цену снапшота; домены со свежим снапшотом из входящего потока считаются бесплатными. Если оценка выше бюджета пачки или остатка месячного лимита, кнопка запуска неактивна, а админка предлагает наибольшее число доменов, которое укладывается. Запуск резервирует оценку в месячном лимите атомарной операцией, чтобы две пачки, запущенные подряд, не превысили месяц.

**Во время прогона.** Если фактическая стоимость пачки дошла до её бюджета, оставшиеся домены получают статус «пропущен: бюджет», и черновик письма для них не готовится. Потолок домена действует как у тизера: жёсткий потолок переводит снапшот в `failed`, домен помечается в пачке. После завершения пачки резерв заменяется фактической стоимостью, остаток возвращается в месячный лимит.

**Что видно в админке.** По каждой пачке: бюджет, потрачено, остаток, число пропущенных доменов, фактическая стоимость на домен. По месяцу: потрачено и лимит. Уведомления идут в очередь админки, а не в алерты раздела 11.3: пачка остановлена бюджетом, месяц израсходовал 80% лимита.

---

## 5. Стратегия тестирования

### 5.1 Три класса кода и разная дисциплина для каждого

Единого правила «тест всегда до кода» не будет: в системе есть части, для которых ожидаемый результат нельзя записать заранее. Поэтому код делится на три класса, и у каждого своя дисциплина.

| Класс | Что входит | Дисциплина |
|---|---|---|
| Детерминированное ядро | Скоринг, правила блокеров, доверительный интервал, приоритизация находок, прогноз скора для корзины, переходы статусов, нормализация URL и доменов, проверка адресов, разбор robots.txt, шаблонизация находок | Строгий порядок: тест до кода. Критерий приёмки задачи формулируется как список падающих тестов |
| Интеграции | Адаптеры движков, краулер, SerpAPI, Paddle, Resend, Storage | Сначала один живой вызов, записанный в фикстуру, затем контрактный тест на фикстуре, затем код |
| Вероятностный слой | Паспорт продукта, детекция категории, целевой персоны и конкурентов, брендовые и клиентские промпты, генерация канонических наборов по локалям, извлечение упоминаний, позиций, тональности, источников, проверка точности описания | Тестов в обычном смысле нет. Вместо них золотой набор, метрика и порог, который проверяется eval-прогоном |

Интерфейс между классами — граница ответственности. Вероятностный слой отдаёт структуру, детерминированное ядро считает по ней баллы. Если внутри скоринга появляется обращение к модели, значит, граница нарушена.

### 5.2 Уровни тестов

| Уровень | Инструмент | Что ловит | Когда выполняется |
|---|---|---|---|
| Модульные | Vitest | Ошибки в формулах, правилах, парсерах, переходах статусов | На каждый PR, менее 30 секунд |
| Контрактные | Vitest на фикстурах | Расхождение адаптера с ожидаемым поведением, забытый повтор при 429 | На каждый PR |
| Интеграционные шаги | Vitest, Postgres в контейнере | Ошибки идемпотентности, записи артефактов, бюджета | На каждый PR |
| Сквозной прогон пайплайна | Vitest, режим фикстур | Разрыв контракта между шагами, ошибки оркестрации | На каждый PR, около двух минут |
| Интерфейс | Playwright | Поломку трёх ключевых сценариев пользователя | На слияние в `main` и ночью |
| Eval вероятностного слоя | Собственный харнесс | Деградацию точности после смены модели или промпта | По расписанию ночью и по метке в PR |
| Живые контракты провайдеров | Собственный харнесс | Изменение внешнего API или цен | Ночью, один вызов на провайдера |

Три ключевых сценария Playwright: тизер от ввода URL до карточки результата; оплата и получение отчёта по ссылке; открытие отчёта по чужому токену завершается отказом. Оплата в тестах воспроизводится подписанным тестовым вебхуком, а не обращением к Paddle: правило «тесты не ходят в сеть» действует и здесь.

### 5.3 Фикстуры

Фикстура — записанный ответ внешнего мира, лежащий в репозитории. Правила:

- Записывается один раз скриптом `pnpm fixtures:record` с реальным ключом, коммитится вместе с задачей.
- Из ответов вычищаются ключи, идентификаторы запросов и всё, что меняется от вызова к вызову и ломает сравнение.
- Пять эталонных сайтов фиксируются целиком: HTML главной, страницы цен, страницы сравнения, документации, robots.txt и sitemap. Эти же пять сайтов используются в регрессионных прогонах.
- Обновление фикстуры — отдельный PR с обязательным объяснением, что изменилось у провайдера. Молчаливое обновление фикстуры под падающий тест запрещено и ловится правилом ревью.

### 5.4 Золотые наборы и пороги

Для вероятностного слоя заводится девять наборов. Разметку делает продакт, это его основная инженерная работа в неделях 2, 4, 5 и 9. Ответы движков для разметки появляются только после задачи T-18, поэтому наборы упоминаний, источников и тональности собираются в два этапа: 30 ответов для приёмки T-19 на неделе 4, полные 100 — к неделе 5.

| Набор | Размер | Метрика | Порог для гейта |
|---|---|---|---|
| Категория продукта | 20 сайтов на неделе 2, 50 на неделе 9 | Доля верных определений при уверенности выше 0,8 | 85%, доля показа окна ниже 40% |
| Конкуренты | 20 сайтов | Доля релевантных в первых пяти | 70% |
| Упоминания бренда в ответе | 30 ответов на неделе 4, 100 на неделе 5 | F-мера | 0,90 |
| Источники цитирования | 30 ответов на неделе 4, 100 на неделе 5 | Точность извлечения домена | 0,95 |
| Тональность | 100 ответов, три класса | Совпадение с разметкой | 0,80 |
| Точность описания продукта, критерий A5 | 50 утверждений | Совпадение с разметкой | 0,85 |
| Упоминания бренда, ro и ru | По 30 ответов на язык, неделя 9 | F-мера по каждому языку | 0,90 |
| Тональность, ro и ru | По 30 ответов на язык, неделя 9 | Совпадение с разметкой по каждому языку | 0,80 |
| Клиентские промпты | 20 платных аудитов | Доля промптов, которые продакт признал реалистичными | 80% |

Румынские и русские подмножества размечает продакт как носитель обоих языков. Они нужны отдельно, потому что малая модель извлечения на румынском и русском может работать хуже, чем на английском, а общий порог это скроет.

Eval-харнесс — это скрипт, который прогоняет набор трижды, берёт медиану метрики, сравнивает с порогом и печатает таблицу расхождений с примерами; тройной прогон гасит шум модели на наборе в 100 примеров. Запускается ночью и по метке `eval` в PR. Смена модели или правка промпта без прогона eval не сливается — это правило в CLAUDE.md и проверка в CI по изменённым путям.

### 5.5 Детерминизм

Три источника недетерминизма гасятся явно. Время: любая функция, зависящая от даты (свежесть контента, TTL кэша, таймауты ожидания), получает время через контекст, а не через `Date.now()`. Случайность: генератор случайных чисел для выборки страниц и промптов принимает seed. Сеть: в тестовом окружении сетевой слой подменяется заглушкой, которая бросает исключение при любой попытке выйти наружу. Третье правило важнее первых двух: оно делает невозможным случайный дорогой вызов в CI.

### 5.6 Что в CI

На каждый PR: типы, линтер, проверка границ пакетов и импортов сети, модульные, контрактные, интеграционные, сквозной прогон, сканер секретов, проверка зависимостей, проверка миграций на применимость к чистой базе, проверка «в диффе нет удалённых или пропущенных тестов». Бюджет — семь минут; Playwright в него не входит.

На слияние в `main`: всё то же плюс три сценария Playwright, затем деплой.

Ночью: eval вероятностного слоя, живые контракты провайдеров, прогон пяти эталонных сайтов на профиле тизера с записью скора и стоимости в таблицу истории, сканер уязвимостей. Раз в неделю — прогон тех же пяти сайтов на стандартном профиле с обходом кэша. Все прогоны помечаются `is_internal`.

Если PR-прогон вылезает за десять минут, тесты режутся или переносятся в ночной, потому что медленный CI агент обходит, а не чинит.

### 5.7 Определение готовности задачи

Задача закрыта, когда выполнены все восемь пунктов: спецификация не изменилась по ходу работы или изменение зафиксировано в ней; критерий приёмки выражен тестами и они зелёные; ни один существующий тест не удалён и не пропущен; изменение инварианта, если оно было, оформлено ADR; результат проверен продактом на живом сайте, а не только на фикстуре; время и стоимость шага укладываются в бюджет профиля из раздела 12; описание PR заполнено по шаблону; новые зависимости, если они появились, названы в описании явно.

---

## 6. Очерёдность разработки

### 6.1 Логика порядка

Порядок работ определяется не удобством написания кода, а тем, где раньше всего вскрываются дорогие ошибки. Для этого проекта таких мест два: контракты между слоями и стоимость внешних вызовов. Отсюда двухчастная логика.

**Сначала скелет.** Первая неделя не производит ни одной пользовательской ценности и целиком уходит на сквозной пустой аудит: форма создаёт запись, событие уходит в оркестратор, оркестратор вызывает две заглушки шагов, шаги пишут артефакты и события, лента прогресса показывает их в браузере, вызов модели-заглушки записывает нулевую стоимость, статусы проходят по таблице переходов. Соблазн начать с краулера велик, и ему нужно сопротивляться: скелет снимает почти все интеграционные сюрпризы за пять дней, а без него они всплывают на неделе шесть, когда переделывать дорого.

**Потом вертикальные срезы.** Дальше каждый столп доводится до конца целиком: сбор фактов, извлечение, баллы, находки, отображение. Горизонтальный порядок — «сначала все краулеры, потом весь скоринг, потом весь интерфейс» — для одного разработчика опаснее, потому что ошибка в контракте обнаруживается через месяц и ломает три блока сразу. Вертикальный срез даёт работающий кусок продукта каждую неделю и естественный критерий приёмки: результат виден на живом сайте.

Из этой логики есть одно намеренное исключение: минимальная админка делается на неделе 2, а не на неделе 8, хотя пользователю она не нужна. Она не витрина, а инструмент отладки: без просмотра артефактов и стоимости шага разбор того, почему аудит дал странный балл, превращается в чтение логов, а это самая дорогая форма работы для одного человека.

### 6.2 Карта зависимостей блоков

| Блок | Зависит от | Тесты, которые пишутся до кода | Артефакт приёмки |
|---|---|---|---|
| Контракты, схема данных, профили | — | Соответствие Zod и Drizzle, чтение профиля | Пустой аудит создаётся и читается |
| Переходы статусов | Контракты | Каждый разрешённый и запрещённый переход, таймауты | Статусы меняются только через `transition` |
| Оркестратор и лента событий | Контракты, переходы | Идемпотентность шага, порядок и монотонность `seq` | Пустой аудит проходит насквозь |
| Учёт стоимости | Контракты | Расчёт цены по прайс-таблице, потолки профиля | Стоимость вызова видна в Langfuse и в базе |
| Адаптеры движков и модели | Контракты, учёт стоимости | Общий контрактный набор на фикстурах | Живой вызов возвращает нормализованный ответ |
| Безопасный сетевой слой | Контракты | Все случаи раздела 9.2 | Ни один запрос воркера не идёт мимо модуля |
| Краулер | Сетевой слой | Лимиты, robots, глубина по профилю | Снимок сайта в Storage, метрики страниц в базе |
| Паспорт, категория, конкуренты, промпты | Краулер, модель | Схемы вывода, структурные проверки наборов | Eval категории выше порога на 20 сайтах |
| Технический чек, столп B | Краулер | Правила B1–B6 на фикстурах, правила блокеров | Два прогона подряд дают одинаковый результат |
| Кэш категорий | Схема данных, канонические промпты | Попадание, промах, TTL, дозапрос повторов | Второй сайт категории стоит дешевле первого |
| Локали и персоны запроса | Схема данных, таксономия | Разбор кода локали, отсутствие смешения локалей, выбор персоны по паспорту | Набор собирается по локали и персоне |
| Клиентский слой | Паспорт, промпты | Клиентские промпты не попадают в кэш, у каждого есть источник | Промпты с источником видны в отчёте |
| Режим без поиска | Адаптеры, кэш | Режим в ключе кэша, отказ движка без поддержки режима | Индикатор в отчёте не меняет скор |
| Опрос движков, столп A | Адаптеры, промпты, кэш | Конкурентность, повторы, деградация при отказе | Скор A согласован с ручным аудитом |
| Извлечение фактов | Модель, опрос движков | Золотые наборы упоминаний, источников, тональности | Метрики выше порогов |
| Скоринг | Извлечение, столп B | Модульные тесты формул и интервала | Пересчёт старого аудита новой версией весов |
| Находки и корзина | Скоринг, шаблоны | Полнота семи полей, приоритизация, прогноз | Находки читаемы без правки в половине случаев |
| Отчёт, PDF | Находки, пакет ui | Отказ по чужому токену, сборка PDF через внутренний маршрут | Отчёт открывается по ссылке, PDF приходит письмом |
| Оплата | Отчёт, профили | Подпись вебхука, идемпотентность, дубль события, возврат | Тестовый платёж создаёт платный аудит |

### 6.3 Критический путь и параллельность

Критический путь: контракты, переходы статусов, оркестратор, адаптеры, опрос движков, извлечение, скоринг, находки, отчёт, оплата. Всё остальное к нему пристраивается.

Чередовать с критическим путём можно без риска: интерфейс лендинга и прогресса, локализацию, письма, юридические страницы, админку, дашборд метрик. Это важно практически: когда продакт занят разметкой золотых наборов и не может быстро принимать задачи по ядру, агент берёт задачу из интерфейсной группы, где приёмка визуальная и занимает минуты. Чередование — не параллельность: для одного разработчика-агента в работе одна ветка, следующая задача начинается после слияния предыдущей.

Нельзя вести подряд без слияния: два шага, меняющих схему данных; две задачи, трогающие один и тот же файл конфигурации.

### 6.4 Точки расширения, закладываемые сейчас

Решение по e-commerce отложено, но стоимость подготовки к нему сейчас близка к нулю, а позже измеряется неделями. Закладываются четыре точки:

- поле `business_type` в `audits` и `categories` с единственным значением `saas` в MVP; профиль задаёт объём аудита, тип бизнеса — состав критериев, это разные измерения;
- паспорт продукта как размеченное объединение типов, а не плоский объект;
- генератор брендовых промптов как выбираемая стратегия по типу бизнеса, с одной реализацией;
- реестр критериев столпа E с признаком применимости к типу бизнеса, чтобы товарные критерии добавлялись данными, а не правкой скоринга.

Аналогично для фазы 2 закладываются две вещи: `engine_runs` пишется в форме, пригодной для последующей выгрузки во временные ряды, и оркестрация изолирована за тонким интерфейсом, чтобы замена Inngest на Temporal не расползлась по коду шагов.

---

## 7. Шаги пайплайна

### 7.1 Общие правила

Пятнадцать функций шагов реализуют двенадцать шагов методологии; шаг 0 методологии (нормализация ввода) выполняется в API создания аудита. Каждая функция подчиняется одним правилам: проверить, есть ли готовый результат по идемпотентному ключу, и не считать повторно; читать объём работы из профиля; писать артефакты в Storage, а ссылки в базу; писать событие в ленту прогресса в начале и в конце; учитывать стоимость каждого внешнего вызова; при отказе внешнего сервиса возвращать `partial` с заполненным полем `notes`, а не бросать исключение; уложиться в бюджет времени и денег профиля.

Параллельность задаётся оркестратором и фиксируется здесь: `prompts.client` идёт после `product.passport` параллельно с `category.detect` и завершается до `prompts.generate`; `tech.check` идёт параллельно с `prompts.generate` и `engines.poll`; прогон без поиска — часть `engines.poll` и идёт параллельно с основным; `sources.analyze` после `engines.poll`; `content.analyze` и `offsite.collect` параллельно после `sources.analyze`; `agent.scripted` параллельно со всем после `site.crawl`; `score.compute` после всех.

### 7.2 Состав шагов

| Шаг | Вход | Выход | Поведение при отказе |
|---|---|---|---|
| `site.crawl` | URL, глубина из профиля | Снимки страниц, метрики, robots, sitemap | Менее трёх страниц — аудит останавливается с внятной ошибкой пользователю |
| `product.passport` | Снимки страниц | Паспорт продукта: что делает, для кого, категория-кандидат, бренд и варианты его написания, включая кириллицу и транслитерацию | Повтор, затем запасная модель, затем ручной ввод в админке |
| `category.detect` | Паспорт, таксономия | Категория, уверенность, целевая персона запроса | Уверенность ниже 0,8 — окно выбора; таймаут 10 минут — автовыбор с пометкой |
| `competitors.detect` | Паспорт, ввод пользователя, число из профиля | Список конкурентов с обоснованием | Без движков и SerpAPI в MVP: сайт, паспорт, знание модели; пустой список допустим |
| `prompts.client` | Паспорт, FAQ и отзывы со снимков сайта, вопросы покупателей из тизера, два запроса SerpAPI по отзывам, локаль | 6 или 10 клиентских промптов на языке локали, у каждого источник | Нет источников — промпты из паспорта с пометкой «модельные формулировки»; в тизере шаг не выполняется |
| `prompts.generate` | Категория, персона, локали профиля, паспорт, конкуренты, клиентские промпты | Категорийные промпты по идентификаторам из канонического набора плюс брендовые и клиентские; режим каждого промпта | Нет набора для категории — набор ближайшей категории или общий «SaaS» той же локали, пометка. Набора в нужной локали нет — аудит в этой локали не запускается, перевод с другой локали не допускается |
| `tech.check` | Домен, robots, снимки, число запросов B4 из профиля | Баллы B1–B6, факты блокеров | Недоступность SerpAPI помечает B4 как неизмеренный |
| `engines.poll` | Промпты, движки и режимы профиля, кэш | Ответы, источники, usage по режимам и локалям | Отказ одного движка помечает столп частичным; движок без режима без поиска в этом прогоне не участвует |
| `sources.analyze` | Ответы движков | Карта источников, домены, частоты | Частичный результат допустим |
| `content.analyze` | Снимки 10 страниц, карта источников | Баллы C1–C6, сравнение с цитируемыми | Частичный результат допустим |
| `offsite.collect` | Бренд, конкуренты | Сигналы D1, D2, D4; сигналы по домену бренда кэшируются на 14 дней и переиспользуются другими аудитами, где этот бренд — конкурент | Лимит SerpAPI помечает критерий неизмеренным |
| `agent.scripted` | Домен, сценарии | Результат двух сценариев, скриншоты, точка отказа | Таймаут сценария — это результат, а не ошибка |
| `score.compute` | Все факты, версия конфигурации | Баллы по критериям и столпам, общий скор, интервал | Не может завершиться частично: либо скор есть, либо аудит в `needs_attention` |
| `findings.build` | Скор, факты, срезы, шаблоны | До 15 находок семи полей, включая находки разрывов (раздел 7.4), корзина, прогноз | Отсутствие шаблона — находка помечается для ручной правки |
| `report.render` | Находки, скор, бенчмарк | Веб-отчёт, отложенный PDF | Ошибка PDF не блокирует веб-отчёт |

### 7.3 Четыре шага, которые надо описать подробно

Остальные шаги описываются спецификациями по шаблону из приложения А. Четыре требуют внимания заранее, потому что именно они определяют себестоимость, воспроизводимость и доверие к продукту.

**`prompts.generate` — основа кэша.** Промпты делятся на три класса.

Категорийные — фиксированный набор из 40 на категорию, локаль и версию, в порядке приоритета: discovery 16, problem-led 10, сравнения между пятью ведущими игроками категории 8, альтернативы лидеру 4, локальные 2. Треть discovery и problem-led — пять и три промпта — несёт контекст персоны запроса и помечена `persona_id`. Персоны, две-три на категорию, и ведущие игроки записаны в таксономии, а не берутся с сайта клиента. Так персонные промпты остаются общими для категории и не ломают кэш, а целевую персону клиента выбирает `category.detect` по паспорту.

Наборы генерируются один раз для всех 200 категорий и четырёх локалей (en, ro-MD, ru-MD, ro-RO) скриптом `pnpm prompts:generate-categories`, коммитятся в `config` и правятся продактом как данные; правка текста — новая версия набора. Генерация идёт на языке локали: промпт задачи генерации написан на целевом языке и требует формулировок местного покупателя. Перевод набора с другой локали запрещён — переведённый промпт звучит как перевод и даёт другую картину ответов. Наборы ro-MD и ro-RO могут совпадать в большей части, но различаются локальными промптами и хранятся отдельно.

Брендовые промпты генерируются под аудит на языке локали: «что такое {Бренд}», «{Бренд} pricing», «{Бренд} alternatives», «{Бренд} reviews», «{Бренд} vs {Конкурент}» по числу конкурентов профиля.

Клиентские промпты — только платные профили. Шаг `prompts.client` собирает формулировки покупателей из трёх источников: вопросы, которые клиент ввёл на странице результата тизера; FAQ, отзывы и кейсы на сайте; сниппеты отзывов из двух запросов SerpAPI. Модель превращает их в вопросы к ассистенту на языке локали и сохраняет источник каждого. Если источников нет, промпты строятся из паспорта и помечаются в отчёте как модельные.

Тизер берёт 10 категорийных по приоритету и 2 брендовых; стандартный — 20, 4 и 6 клиентских; расширенный — 30, 10 и 10; дополнительная локаль расширенного — 20 категорийных и 5 брендовых. Прогон без поиска берёт discovery-промпты по приоритету и брендовые в объёме из раздела 4.7. Брендовые и клиентские промпты не кэшируются никогда. Список конкурентов сайта используется для извлечения упоминаний и брендовых промптов, но не влияет на категорийные.

**`engines.poll` — главный потребитель денег и времени.** Вход: список промптов, движки профиля, число повторов. Логика: сначала проверка кэша по ключу «категория, локаль, версия набора, идентификатор промпта, движок, режим»; брендовые и клиентские промпты никогда не берутся из кэша; категорийные берутся, если запись моложе 14 дней и содержит нужное число повторов, иначе дозапрашиваются недостающие. Конкурентность задаётся ключом провайдера, а не глобально, чтобы лимит одного движка не тормозил остальные. При 429 — экспоненциальная задержка с четырьмя попытками; при исчерпании попыток движок помечается частичным на этом промпте. Каждый ответ сохраняется целиком в артефакты: без сырого ответа невозможно ни разобрать жалобу, ни пересчитать извлечение новой моделью. Прогон без поиска — отдельные вызовы с `search: false` на движках профиля, которые поддерживают этот режим; при необходимости адаптер передаёт страну рынка из локали. Тесты: контрактный набор на фикстурах, тест на попадание, промах и дозапрос повторов, тест на деградацию при полном отказе одного движка, тест на соблюдение потолка профиля.

**`score.compute` — сердце воспроизводимости.** Чистая функция без обращений к сети и базе. Вход: структурированные факты и версия конфигурации. Выход: балл по каждому из 29 критериев, балл по столпам, общий скор, доверительный интервал по формуле раздела 4.5, список неизмеренных критериев, применённые блокеры. Правило: неизмеренный критерий не считается нулём — он исключается из знаменателя столпа и расширяет интервал. Это принципиально: иначе временный сбой SerpAPI превращается в «у вас ноль по внешнему следу» и уничтожает доверие к отчёту. Правило блокера применяется только если все критерии, на которые оно опирается, измерены: при отключённом E1 блокер по сценарию «найти цену» не применяется. Тесты пишутся до кода и покрывают: сумму весов, полосы интерпретации, каждое правило блокера и его отключение, поведение при неизмеренных критериях, интервал при одном и двух повторах, устойчивость к выходу значения за диапазон, пересчёт исторического аудита другой версией конфигурации.

**`category.detect` — узкое место качества.** От него зависит релевантность промптов, а значит, и осмысленность всего аудита. Реализация: малая модель получает паспорт продукта и список категорий-кандидатов из таксономии, возвращает категорию, уверенность и обоснование одной фразой. Ниже 0,8 — окно выбора у пользователя с тремя вариантами и полем «другое»; через 10 минут без ответа — автовыбор кандидата с наибольшей уверенностью и пометка в отчёте. Метрика и порог — в разделе 5.4. Важное продуктовое требование: доля показа окна отслеживается как метрика, и её рост выше 40% является сигналом расширять таксономию, а не менять модель. Тот же вызов выбирает целевую персону запроса из персон категории; при низкой уверенности персона не выбирается, и срез по персонам показывается без выделения «вашей».

### 7.4 Срезы отчёта и находки разрывов

Версия 1.2 даёт четыре среза столпа A: по режиму (с поиском и без), по персоне запроса, по локали и по слою промптов (категорийный против клиентского). Срез показывается, только если в нём шесть промптов и больше, — на меньшей выборке разница выглядит как находка, а является шумом.

Разрыв — разница доли упоминаний между частями среза больше порога из конфигурации; стартовое значение 20 процентных пунктов, уточняется калибровкой. Каждый тип разрыва — шаблон находки со своей маршрутизацией к услугам:

| Разрыв | Что значит | Услуги |
|---|---|---|
| Видны с поиском, не знают без поиска | Бренд не закреплён в памяти моделей | Entity Consistency Kit, Review Footprint Setup, Digital PR |
| Знают без поиска, не видны с поиском | Страницы не извлекаются | AI Crawl Fix, Citable Content Pack |
| Видны одной персоне, не видны целевой | Контент не отвечает на сценарии целевой персоны | Comparison & Alternatives Hub с use-case-страницами |
| Видны в одной локали, не видны в другой | Нет локального контента и локальных площадок | Локальные версии ключевых страниц, Review Footprint Setup |
| Клиентские промпты хуже категорийных | Сайт не отвечает на реальные вопросы покупателей | Citable Content Pack под эти вопросы |

Порог, минимальный размер среза и маршрутизация лежат в `config` рядом с шаблонами находок и правятся продактом как данные.

---

## 8. Работа с моделями внутри продукта

### 8.1 Роутер моделей

Даже в MVP слой извлечения должен быть модельно-независимым, иначе сравнение Gemini Flash с альтернативами на неделе 9 превращается в переписывание кода. Роутер — это таблица соответствия «задача, модель, запасная модель» в `config` и одна функция, которая по имени задачи отдаёт адаптер. Задач девять: паспорт, категория с персоной, конкуренты, брендовые промпты, клиентские промпты, генерация канонических наборов, извлечение упоминаний, тональность, текст находки. Варианты написания бренда строятся детерминированной транслитерацией, модель паспорта только дополняет их. Смена модели для задачи — правка одной строки конфигурации и прогон eval.

**Модель для адаптера ChatGPT** — среднего класса (на 11 сентября 2026 — GPT-5.6 Luna), не флагман. По расчёту себестоимости флагман удваивает стоимость промаха кэша и ломает потолок стандартного профиля. На неделе 5 расхождение упоминаний между средней моделью и флагманом замеряется на пяти эталонных сайтах; решение о переходе на флагман принимается по этому замеру и фиксируется ADR.

**Модель для адаптера Claude** — Claude Haiku 4.5 с инструментом веб-поиска и ограничением в один поиск на вызов (`max_uses: 1`). Claude сам решает, искать ли и сколько раз; без ограничения число поисков на вызов растёт, а с ним и цена. Режим без поиска для Claude не используется: этот срез остаётся на ChatGPT и Gemini. Пользователи приложения Claude по умолчанию работают на более старших моделях, поэтому на неделе 5 расхождение упоминаний между Haiku и моделью среднего класса замеряется на пяти эталонных сайтах, как для ChatGPT; решение фиксируется ADR.

### 8.2 Промпты как код

Промпты задач модели живут в `packages/config/prompts/tasks` в виде файлов с версией в имени, по одному на задачу и локаль; канонические наборы категорий — в `packages/config/prompts/categories`. Требования: никакой конкатенации строк в коде шага; переменные подставляются шаблонизатором; изменение файла промпта требует прогона eval; в истории репозитория видно, какой промпт действовал в момент конкретного аудита, а в `audits` пишется версия набора. Без последнего невозможно объяснить, почему аудит месячной давности отличается от сегодняшнего. Язык промпта задачи совпадает с языком локали, для которой генерируется результат: промпты задач для ro-MD и ru-MD написаны на румынском и русском, а не на английском с пометкой «ответь по-русски».

### 8.3 Стоимость, потолки и деградация

Три уровня защиты бюджета. Первый — потолки профиля: мягкий поднимает алерт, жёсткий останавливает опрос движков; для тизера жёсткий потолок переводит аудит в `failed`, для платного — использует собранные ответы, помечает столп частичным и переводит аудит в `needs_attention` для просмотра перед доставкой. Второй — дневной потолок на весь сервис: при превышении новые бесплатные тизеры перестают приниматься, платные аудиты продолжают выполняться. Третий — лимит расходов, выставленный на стороне каждого провайдера: это последняя защита от ошибки в нашем коде, и она не зависит от нашего кода.

Дешёвые проверки всегда идут раньше дорогих. Домен не резолвится, сайт отдаёт 500, страниц меньше трёх — деньги не тратятся вообще. Email запрашивается до brand-промптов, то есть до самой дорогой части тизера.

**Доставка платного отчёта при сбоях.** Обещание клиенту — отчёт в течение 24 часов, обычно в течение часа. Сразу после вебхука уходит письмо «оплата получена, отчёт готовим» со ссылкой на страницу прогресса. Четыре класса сбоев и правило для каждого:

| Класс | Пример | Правило |
|---|---|---|
| Частичный столп | Движок отдал 429 на всех попытках, SerpAPI исчерпан | Аудит завершается, столп помечен частичным, интервал расширен. Отчёт доставляется. Админ может перезапустить шаг позже — отчёт пересоберётся, клиенту уходит письмо «отчёт дополнен». Без возврата |
| Жёсткий потолок стоимости | $8 достигнут на опросе движков | Опрос останавливается, собранные ответы используются, дальше как частичный столп. Статус `needs_attention` до доставки |
| Фатальный сбой | `score.compute` не собрался, зависание более 15 минут | `needs_attention`, алерт. Админ в течение 24 часов: перезапуск шага или возврат одной кнопкой через API Paddle с автоматическим письмом клиенту |
| Ручная проверка | Продакт недоступен | Окно проверки 24 часа. Если аудит без частичных столпов и без признаков инъекции, по истечении окна отчёт отправляется автоматически. Иначе ждёт |

Правило возврата, фиксируется в условиях: полный возврат, если отчёт не доставлен за 48 часов или после фатального сбоя; частичный столп возврата не даёт. Автоотправка после 24 часов — единственное отступление от правила «ручная проверка не сокращается»: держать оплаченный отчёт дольше суток из-за возможной опечатки вредит больше, чем опечатка; продакт может править после доставки, отчёт пересоберётся.

### 8.4 Кэш категорий

Главный рычаг себестоимости и одновременно главный источник тонких ошибок. Ключ: `category_id, locale, prompt_set_version, prompt_id, engine_id, mode`; значение — список ответов по повторам. Локаль в ключе — пара «язык плюс рынок»: ответы ro-MD и ro-RO хранятся раздельно, потому что с поиском они различаются. TTL 14 дней. Брендовые и клиентские промпты не кэшируются никогда. Кэш хранится в Postgres, а не в Redis, потому что те же данные читаются для бенчмарков и аутрича. Обязательные тесты: попадание, промах, истечение, дозапрос недостающих повторов, отсутствие утечки брендовых и клиентских промптов в кэш, отсутствие смешения локалей, включая ro-MD и ro-RO, отсутствие смешения режимов, отсутствие смешения версий набора. Последние три — именно те ошибки, которые тихо портят отчёты и обнаруживаются через месяц.

Проверки воспроизводимости (G2, T-48) выполняются с обходом кэша: повтор по кэшу совпадает тривиально и ничего не проверяет.

**Извлечение один раз на категорийный ответ** (версия 1.3). Ответ на категорийный промпт одинаков для всех аудитов категории, поэтому модель разбирает его один раз — в момент записи в кэш — и извлекает сразу всё: упомянутые бренды с позициями, источники, тональность каждого упоминания. Результат хранится в записи кэша рядом с ответом. Аудит при попадании в кэш не вызывает модель для категорийных ответов: он сопоставляет варианты написания своего бренда и брендов конкурентов с уже извлечёнными сущностями детерминированным кодом в `core`. Модель вызывается только для брендовых, клиентских и некэшируемых ответов, а также для критерия A5, который зависит от паспорта конкретного продукта. Если бренд не нашёлся среди извлечённых сущностей, но совпал по варианту написания в тексте ответа, упоминание засчитывается и помечается для проверки — это страховка от пропуска малоизвестного бренда при общем извлечении. Смена модели или промпта извлечения — новая версия извлечения в записи кэша; старые записи переизвлекаются по мере обращения.

### 8.5 Дрейф движков

Движки меняют поведение без предупреждения, и это не риск, а данность: доля Reddit в источниках ChatGPT падала в шесть раз за два месяца. Механика наблюдения простая: ночной прогон пяти эталонных сайтов на профиле тизера и еженедельный на стандартном профиле пишут скор, состав источников и стоимость в таблицу истории; расхождение скора эталонного сайта больше чем на 0,5 балла без наших изменений — сигнал разобраться. Ежедневный прогон на стандартном профиле стоил бы около $580 в месяц — в несколько раз больше всех платных аудитов, поэтому он еженедельный. Еженедельный прогон идёт с обходом кэша, то есть по цене промаха: для семи эталонных сайтов (пять исходных и два из T-68) с четырьмя движками это около $114 в месяц, вместе с ежедневным прогоном на профиле тизера — около $129 (GEOTRACK_UNIT_ECONOMICS_CHECK_RU.md, версия 1.4). Если статья станет заметной, первый рычаг — прогон стандартного профиля раз в две недели, около $72. Это же даёт материал для честной коммуникации с клиентом при повторном аудите.

---

## 9. Безопасность

### 9.1 Модель угроз

GeoTrack отличается от обычного веб-сервиса тремя свойствами, и каждое создаёт свой класс угроз: сервис по запросу анонимного пользователя ходит на произвольный адрес в интернете; каждый запрос стоит реальных денег; содержимое чужих сайтов попадает в контекст языковой модели. Разбор по поверхностям:

| Поверхность | Основные угрозы | Основная защита |
|---|---|---|
| Публичная форма аудита | Обращение к внутренним адресам, исчерпание бюджета, автоматизированное злоупотребление | Проверка адреса, лимиты, Turnstile, потолки стоимости |
| Краулер и воркер | Подмена адреса при редиректе и между резолвом и соединением, бесконечные страницы, вредоносный контент | Единый сетевой модуль с закреплением адреса, фабрика Playwright, изоляция воркера, лимиты |
| Слой моделей | Инъекция инструкций из контента сайта и из ответов движков | Модель без инструментов, жёсткая схема вывода, баллы считает код |
| Отчёт по ссылке | Подбор токена, индексация поисковиками, утечка через реферер | Токен 32 байта, хранение хэша, запрет индексации, политика реферера |
| Вебхуки платежей | Подделка события, повтор события, подмена суммы | Проверка подписи, идемпотентность по идентификатору события, цена с сервера |
| Админка | Несанкционированный доступ, дорогие действия | Аутентификация, роль, RLS, журнал действий |
| Секреты и зависимости | Утечка ключа, подмена пакета | Разделение окружений, сканеры, фиксированный lock-файл |
| Разработка агентом | Незамеченная ошибка, отключённый тест, лишняя зависимость | Автоматические гейты, запрет автослияния, правила в CLAUDE.md |

### 9.2 Исходящие запросы воркера

Пользовательский URL — основной вход продукта, и обращение к внутренним адресам — его главная техническая уязвимость. Защита реализуется в двух местах, и других способов выйти в сеть у воркера нет — это проверяется линтером по импортам.

**Модуль `core/net/safe-fetch` для запросов через undici.** Хост резолвится во все адреса; запрос отклоняется, если хотя бы один адрес в запрещённых диапазонах: IPv4 приватные, петля, 169.254.0.0/16 включая адрес метаданных, 100.64.0.0/10, 0.0.0.0/8, multicast; IPv6 ::1, fc00::/7, fe80::/10 и IPv4-отображённые адреса. Соединение устанавливается на проверенный адрес через собственный `lookup` в агенте undici, SNI и заголовок Host — по имени: так закрывается подмена адреса между резолвом и соединением. Редиректы отключены в клиенте и обрабатываются вручную: до пяти, с полной проверкой на каждом. Схемы http и https, порты 80 и 443, таймаут 15 секунд, тело 2 мегабайта, одна секунда между запросами к одному домену, общий лимит страниц по профилю.

**Фабрика контекстов Playwright.** Контекст создаётся только фабрикой, которая ставит перехват `route` на все запросы: запрещены схемы кроме http и https, порты кроме 80 и 443, IP-литералы в хосте; имя хоста проверяется тем же резолвером с кэшем на аудит и пропускается только при успехе; ресурсы типов media, font и websocket блокируются. Остаточный риск подмены между резолвом в Node и резолвом Chromium закрывается изоляцией воркера.

**Изоляция воркера.** Приватная сеть Railway для воркера отключена; единственные адресаты внутри инфраструктуры — Postgres и Storage по учётным данным; эндпоинт Inngest на самом воркере принимает только подписанные запросы. Проверка при старте воркера: запрос к адресу метаданных и к приватному диапазону должен завершаться отказом, иначе воркер не поднимается.

Тесты пишутся до кода: резолв в приватный адрес; редирект с публичного адреса на приватный; хост с двумя адресами, один из которых приватный; IPv4-отображённый IPv6; IP-литерал в Playwright; схемы `file:` и `data:` в Playwright; порт 8080; шестой редирект; тело больше лимита; попытка импорта undici мимо модуля.

### 9.3 Инъекция инструкций из контента

Краулер приносит текст чужого сайта, а движки — ответы, в которых тоже может быть что угодно. Всё это попадает в контекст модели. Владелец сайта, желающий завысить свой скор, может разместить в HTML текст, адресованный нашей модели.

Защита выстроена слоями, и первый из них архитектурный:

1. **Баллы считает код, а не модель.** Модель возвращает структуру: упомянут ли бренд, на какой позиции, какие домены в источниках. Убедить её вернуть «оценка 10 из 10» бесполезно — такого поля нет.
2. Схема вывода максимально ограничена перечислениями и числами, свободный текст допускается только там, где он попадает в отчёт как цитата и экранируется.
3. Модель не получает инструментов, не ходит в сеть, не пишет в базу.
4. Перед передачей в модель HTML очищается: удаляются скрипты, стили, комментарии, скрытые элементы и элементы с нулевым размером — классическое место для спрятанного текста.
5. Недоверенный текст передаётся в отдельном размеченном блоке с явным указанием, что это данные, а не инструкции, и обрезается по длине.
6. Подозрительные образцы («игнорируй предыдущие инструкции» и подобные) логируются как признак попытки, такие аудиты видны в админке и не отправляются автоматически по истечении окна проверки.

Отдельное правило: текст, извлечённый со стороннего сайта, никогда не попадает в отчёт и в письмо без экранирования. Находка собирается из шаблона, модель заполняет слоты.

### 9.4 Защита бюджета от злоупотреблений

Бесплатный тизер — это кнопка «потратить деньги владельца сервиса», и относиться к ней нужно соответственно. Контур: Turnstile на форме; один бесплатный тизер на нормализованный домен в 30 дней; лимит запросов по IP; ограничение глубины очереди счётчиком в Redis на стороне API создания аудита; потолки профиля; дневной потолок на сервис с отключением бесплатного потока; дешёвые проверки до дорогих. Нормализация домена обязана снимать поддомены-обёртки и регистр, иначе лимит обходится тривиально. Внутренние прогоны обходят лимиты по служебному ключу и помечаются `is_internal`.

### 9.5 Доступ к данным

Отчёт доступен по ссылке без регистрации — это продуктовое решение, и его нужно реализовать аккуратно. **Токен отчёта — единственный механизм доступа к отчёту.** Он генерируется криптографическим генератором, 32 байта, URL-безопасное представление. В базе хранится хэш токена, а не сам токен: утечка дампа не даёт доступа к отчётам. Страницу собирает сервер по хэшу; RLS защищает не отчёт, а админку и данные пользователей. Страница отчёта отдаёт заголовки запрета индексации и строгую политику реферера, чтобы токен не утёк через переходы по внешним ссылкам внутри отчёта. Неуспешные попытки доступа ограничиваются по частоте. Лента прогресса читается по отдельному короткоживущему токену прогресса, выданному при создании аудита.

**Email-гейт.** Ввод email продолжает пайплайн сразу, без подтверждения: пользователь остаётся на экране прогресса. Magic link в письме нужен только для повторного доступа к результату и создаёт запись в `users`.

**PDF.** Воркер печатает страницу отчёта через внутренний маршрут рендера с сервисным секретом, без пользовательского токена, лимитов и политики реферера.

Админка закрыта аутентификацией и ролью, политики RLS написаны по принципу «запрещено по умолчанию», а действия, которые стоят денег или возвращают их (перезапуск шага, повторный аудит, возврат платежа), пишутся в журнал с указанием, кто и когда их выполнил.

### 9.6 Платежи

Правила, каждое из которых закрывается тестом: подпись вебхука проверяется всегда, включая тестовое окружение; обработка идемпотентна по идентификатору события провайдера и по заказу, повторная доставка не создаёт второй аудит; цена и состав покупки определяются на сервере по идентификатору продукта, значения из браузера не принимаются; переход заказа в статус «оплачено» и создание платного аудита происходят только по вебхуку, а не по возврату пользователя на страницу успеха; сырое событие сохраняется целиком для разбора спорных случаев; письмо подтверждения оплаты уходит из обработчика вебхука; возврат выполняется из админки через API Paddle и пишется в журнал.

### 9.7 Секреты и цепочка поставок

Секреты только в переменных окружения, разделённых по окружениям; сканер секретов в CI и на pre-commit; ротация при любом подозрении и раз в квартал; на стороне провайдера у каждого ключа выставлен лимит расходов. Зависимости: lock-файл фиксирован, установка в CI только с флагом заморозки, проверка уязвимостей и проверка новых зависимостей на каждом PR, статический анализ безопасности. Отдельное правило под LLM-разработку: любая новая зависимость называется в описании PR явно и подтверждается человеком — агенты периодически предлагают несуществующие пакеты, а такие имена регистрируют злоумышленники.

### 9.8 Безопасность самого процесса разработки

Человек не прочитает весь код, значит, защита переносится в автоматику и в правила:

- ветка `main` защищена, автослияния нет, слияние только при зелёном CI;
- агент работает только с dev-окружением и dev-ключами, продакшен-доступа у него нет;
- отдельная проверка в CI: в диффе нет удалённых, закомментированных или пропущенных тестов. Это самый частый способ сделать CI зелёным, не решив задачу;
- миграции, изменения в контуре безопасности, правки конфигурации весов, профилей и переходов, подключение нового провайдера всегда читает человек;
- секреты и токены не попадают в логи: правило и проверка по шаблонам в тестах логирования;
- ревью выполняет отдельная сессия агента, которая видит спецификацию и дифф, но не видит рассуждений автора.

### 9.9 Правовой и приватный контур

Краулим сайт, который пользователь ввёл сам, и публичные страницы источников из ответов движков. Собственный бот представляется как `GeotrackBot` и уважает robots.txt. Запросы с user-agent AI-ботов выполняются в рамках критериев B1 и B2 как измерение, а не обход: одиночные запросы, не чаще одного в секунду, остановка при 429, и метод описан на публичной странице методологии — это защита и репутационная, и юридическая.

Скриптовые сценарии столпа E в MVP выполняются только на домене, который аудитируется, только на чтение, без отправки форм с реальными данными и без каких-либо платёжных шагов; сценарий регистрации останавливается на первом барьере, а не проходит его. Полноценный агентный прогон в фазе 3 требует подтверждения владения доменом.

Персональные данные в MVP — email и домен. Срок хранения 12 месяцев; функция удаления по запросу пишется сразу; черновые условия, политика конфиденциальности и политика возвратов публикуются на домене на неделе 1–2 для заявки в Paddle, финальные тексты — на неделе 9; DPA и регион ЕС — вопрос фазы 3, и до него mid-market не продаётся.

### 9.10 Чек-лист

Полный чек-лист перед деплоем — в приложении Е. Он проходится не при каждом слиянии, а перед каждым внешним событием: первый публичный тизер на неделе 4, включение оплаты на неделе 8, начало беты на неделе 10.

---

## 10. Операционная модель разработки с Claude Code

### 10.1 Цикл одной задачи

Шесть шагов, каждый со своим артефактом:

1. **Спецификация.** Продакт пишет файл в `docs/specs/` по шаблону из приложения А. Ключевая часть — проверяемый критерий приёмки и раздел «что не делаем». Спецификация ссылается на разделы документов проекта, а не пересказывает их.
2. **Уточнение.** Агент читает спецификацию и возвращает список вопросов и предполагаемых решений до написания кода. Три-пять вопросов на этом шаге экономят час переделки.
3. **Тесты.** Для детерминированного ядра сначала пишутся падающие тесты, и их вывод прикладывается к описанию PR. Для интеграций сначала записывается фикстура.
4. **Реализация.** Код в границах, перечисленных в спецификации. Выход за границы — повод остановиться и вернуться к шагу 1, а не расширять задачу.
5. **Ревью.** Отдельная сессия агента получает спецификацию и дифф и отвечает на четыре вопроса: соответствует ли код критерию приёмки, что осталось непокрытым тестами, есть ли нарушения инвариантов, есть ли риски из раздела 9.
6. **Приёмка.** Продакт читает описание PR, вывод тестов и заключение ревью, запускает результат на живом сайте и сливает.

### 10.2 Контекст: что агент читает и когда

Главная проблема разработки агентом — не качество кода, а потеря контекста между сессиями. Решается распределением знаний по трём местам с разным сроком жизни:

| Место | Что там лежит | Срок жизни |
|---|---|---|
| CLAUDE.md | Соглашения, инварианты, команды, запреты, карта репозитория | Постоянно, обновляется еженедельно |
| `docs/adr/` | Принятые решения и их причины, по странице на решение | Постоянно, только дополняется |
| `docs/specs/` | Текущая задача и её границы | Неделя |
| `docs/product/` | Документы проекта | Постоянно, читаются выборочно по ссылке из спецификации |

Правило: в чат не пересказывается то, что есть в файле. Пересказ создаёт вторую версию истины, которая живёт одну сессию и потом противоречит репозиторию. CLAUDE.md держится в пределах двух страниц: длинный файл агент читает по диагонали.

### 10.3 Разделение ролей между сессиями

Четыре роли, каждая — отдельная сессия:

- **Постановщик** превращает раздел документа в спецификацию с критерием приёмки. Работает вместе с продактом.
- **Исполнитель** пишет тесты и код по спецификации. Не меняет спецификацию сам.
- **Ревьюер** проверяет дифф против спецификации. Не видит контекста реализации — именно поэтому замечает то, что исполнитель считает очевидным.
- **Архитектор** раз в неделю обновляет CLAUDE.md и ADR, ищет расхождения между документами и кодом, предлагает, что вынести в `core`.

Критерий приёмки формулирует постановщик до реализации. Если его формулирует исполнитель после, тесты подгоняются под написанный код, и вся система гарантий перестаёт работать.

### 10.4 Когда человек обязателен

Список триггеров, при которых работа останавливается и зовётся человек — продакт, а в четырёх последних случаях, возможно, и внешний инженер на консультацию:

- миграция удаляет или переписывает данные;
- меняются веса, пороги, формула интервала, правила блокеров, профили или таблица переходов;
- подключается новый внешний провайдер или меняется схема оплаты;
- меняется что-либо в контуре безопасности из раздела 9;
- аудит попал в `needs_attention`;
- eval вероятностного слоя ушёл ниже порога, и непонятно почему;
- продакшен-инцидент: разбор логов и трасс делает человек, агент получает уже собранный контекст;
- себестоимость аудита выросла выше потолка без изменений с нашей стороны;
- две недели подряд закрывается меньше половины запланированных задач.

### 10.5 Технический долг без второго инженера

Три механизма вместо человеческого ревью. Первый — регрессионные прогоны пяти эталонных сайтов с записью скора, времени и стоимости в историю; расхождение — сигнал разобраться до, а не после релиза. Второй — фиксированный бюджет: последний день каждой второй недели уходит на долг, тесты и обновление CLAUDE.md, и этот день не отдаётся под функции. Третий — список отложенного в `docs/specs/debt.md`, куда агент обязан писать всё, что он сознательно упростил; без такого списка упрощения теряются и всплывают как ошибки.

Признаки, что пора звать инженера: регрессии на эталонных сайтах чаще раза в две недели; больше трёх правок подряд в одном файле без прогресса; агент предлагает архитектурное изменение, которое вы не можете оценить; любая тема из фазы 3, связанная с наблюдаемостью, регионом данных и агентным слоем.

### 10.6 Антипаттерны

Шесть способов потерять неделю, каждый проверен практикой:

- задача размером со столп вместо задачи размером в шаг;
- «почини CI» без диагноза: агент отключает тест;
- правка схемы данных попутно, внутри задачи про интерфейс;
- одна длинная сессия на несколько задач: контекст размывается, ранние решения теряются;
- копирование кода между `web` и `worker` вместо выноса в `core`;
- оптимизация без замера: переписанный шаг, который не был узким местом.

### 10.7 Как это выглядит на одной задаче

Задача T-17, кэш категорий. Понедельник, полтора часа времени продакта.

Продакт пишет спецификацию: ключ кэша по идентификаторам, срок годности, что не кэшируется, дозапрос повторов, критерий приёмки «второй сайт той же категории стоит дешевле первого, брендовые промпты в кэш не попадают». Ссылка на раздел 8.4 и на ADR-011, без пересказа.

Сессия-исполнитель читает спецификацию и CLAUDE.md, возвращает четыре вопроса: что делать при смене версии набора промптов; кэшировать ли ответ с ошибкой; как считать стоимость дозапрошенного повтора; нужна ли ручная очистка из админки. Продакт отвечает тремя строками, четвёртый пункт отправляет в отложенное.

Исполнитель пишет пять тестов — попадание, промах, истечение, дозапрос повторов, отсутствие брендовых промптов в кэше, — показывает их падение, затем реализацию, затем зелёный прогон. Открывает PR по шаблону.

Сессия-ревьюер получает спецификацию и дифф. Находит, что ключ кэша не включает локаль, хотя в спецификации это оговорено: румынский ответ вернётся на английский запрос. Исполнитель правит, добавляет тест на смешение локалей.

Продакт запускает аудит двух сайтов одной категории, видит в админке стоимость второго вдвое ниже, читает описание PR и сливает. Задача закрыта за полтора часа его времени, из которых час ушёл на спецификацию и ответы на вопросы.

---

## 11. Окружения, релизы и наблюдаемость

### 11.1 Окружения

Три: локальное на фикстурах без ключей, preview на каждый PR с собственной веткой базы и тестовыми ключами провайдеров, прод. Сиды одинаковые. Переменные окружения описаны в одном файле-примере, и расхождение состава между окружениями проверяется при старте приложения: сервис не поднимается с неполной конфигурацией, а не падает потом на первом запросе. Воркер дополнительно проверяет сетевую изоляцию по разделу 9.2. Аудиты из preview-окружений помечаются `is_internal`.

### 11.2 Релизы, миграции и откат

Слияние в `main` деплоит веб и воркер. Миграции применяются до деплоя кода и пишутся совместимыми в обе стороны: сначала добавляется колонка, потом код начинает её писать, и только следующим релизом старая колонка убирается. Это даёт возможность отката кода без отката базы — единственный реалистичный способ откатиться для одного человека.

Фич-флаги через PostHog нужны для четырёх вещей: включение платного потока, переключение движков, включение румынской локали интерфейса и включение локалей промптов ru-MD и ro-RO. Переключение движка флагом, а не деплоем, — это то, что позволит пережить отказ провайдера за минуты.

### 11.3 Наблюдаемость

Четыре инструмента с непересекающимися задачами: Sentry — ошибки и их частота; PostHog — воронка и продуктовые события; Langfuse — трассы вызовов моделей, токены и стоимость; собственный дашборд — себестоимость на аудит, время по шагам, доля частичных столпов, доля показа окна категории, доля попаданий в кэш по локалям, доля клиентских промптов с пометкой «модельные».

Обязательные алерты в первые недели всего пять, чтобы не приучить себя их игнорировать: аудит попал в `needs_attention`; аудит выполняется дольше 15 минут; дневная стоимость превысила порог; доля неуспешных аудитов за час выше 10%; вебхук платежа не обработан.

### 11.4 Рантбуки

Шесть сценариев, по половине страницы каждый, лежат в `docs/runbooks/`: движок массово отдаёт 429 или пятисотые; себестоимость аудита превысила потолок; пайплайн завис на шаге; клиент оплатил, а вебхук не дошёл; клиент оплатил, а отчёта нет; утёк ключ. Пятый пишется вместе с T-45, остальные — на неделе 9. Каждый рантбук отвечает на три вопроса: как понять, что это оно; что сделать сейчас; что починить потом.

---

## 12. Бюджеты времени и стоимости

### 12.1 Время

Обещание пользователю: результат тизера — за две минуты машинного времени, без учёта ожидания выбора категории и ввода email; платный отчёт — в течение 24 часов, обычно в течение часа. Внутренний бюджет машинного времени: тизер 90 секунд, стандартный аудит 6 минут, расширенный 8 минут. Распределение бюджета стандартного аудита при нормальной работе провайдеров:

| Фаза | Бюджет | Чем определяется |
|---|---|---|
| Разведка сайта и паспорт | 60–90 секунд | Один запрос в секунду на домен, 50 страниц, пять параллельных потоков не нарушают лимит |
| Технический чек, параллельно с опросом | 60–120 секунд | Запросы с восемью user-agent к 10 страницам, повтор через минуту, 11 запросов SerpAPI |
| Опрос движков | 120–180 секунд | 240 вызовов при конкурентности 12 на провайдера; провайдеры опрашиваются параллельно, поэтому четвёртый движок время почти не добавляет |
| Извлечение фактов | 20–40 секунд | Пакетная обработка ответов, десять на вызов |
| Контент и внешний след, параллельно | 40–60 секунд | Ограничены лимитом SerpAPI |
| Скриптовые сценарии, параллельно с опросом | до 60 секунд | Два сценария по 30 секунд с таймаутом |
| Клиентский слой, параллельно с категорией | 15–25 секунд | Два запроса SerpAPI и один вызов модели |
| Прогон без поиска, параллельно с опросом | 20–40 секунд | 20 вызовов без платы за поиск |
| Скоринг, находки, отчёт | 20–30 секунд | Один вызов модели на текст находок |

С учётом параллельности из раздела 7.1 критический путь — 280–400 секунд; новые шаги версии 1.2 идут параллельно и его не удлиняют. Каждая дополнительная локаль расширенного профиля добавляет около двух минут опроса. PDF собирается после показа отчёта и в бюджет не входит. Тизер укладывается в 90 секунд за счёт трёх страниц, одного движка, 12 промптов и двух запросов SerpAPI.

### 12.2 Деньги

Себестоимость пересчитана 11 сентября 2026 по официальным ценам провайдеров (GEOTRACK_UNIT_ECONOMICS_CHECK_RU.md); объёмы токенов на вызов — оценка, замер — на неделе 5.

| Профиль | Первый сайт в категории | Кэш категории есть | Мягкий и жёсткий потолок |
|---|---|---|---|
| Тизер | $0,17 | $0,07 | $0,10 и $0,25 |
| Стандартный $79 | $4,90 | $2,37 | $6 и $8 |
| Расширенный $199, одна локаль | $8,09 | $4,24 | $9 и $12 |
| Каждая дополнительная локаль | плюс $3,50 | плюс $0,98 | плюс $4 и $5 |

Версия 1.2 поднимает себестоимость с кэшем сильнее, чем без него: клиентские промпты не кэшируются по определению, поэтому стандартный аудит с кэшем дорожает с $1,30 до $1,87. Прогон без поиска стоит мало — около $0,04 на стандартном профиле вместе с извлечением. Для локалей ro-MD, ru-MD и ro-RO кэш заполняется медленнее, чем для en: категорий SaaS с клиентами в этих локалях мало, поэтому первые месяцы себестоимость там планируется по колонке «первый сайт». Цифры версии 1.2 — оценка по тем же ценам 11 сентября, расчёт по статьям — в GEOTRACK_UNIT_ECONOMICS_CHECK_RU.md, версия 1.1; замер — на неделе 5 вместе с T-29.

Структура затрат стандартного аудита при промахе кэша: движки с поиском $3,78, из них Claude $1,20; без поиска $0,02, SerpAPI $0,53, извлечение $0,44, остальное $0,13. Три факта, которые меняют расчёт по сравнению с методологией: Gemini даёт 5 000 grounding-запросов в месяц бесплатно, и на объёмах фазы 1 это самый дешёвый движок; цена Gemini Flash удваивается с 1 января 2027; SerpAPI — единственная платная часть тизера, не зависящая от кэша, поэтому B4 в тизере ограничен двумя запросами. Флагманская модель в адаптере ChatGPT переводит промах кэша стандартного аудита в $7,96, а расширенного с тремя локалями — выше жёсткого потолка $20; отсюда решение раздела 8.1.

Рычаги снижения в порядке применения, если бюджет не сойдётся: увеличить долю попаданий в кэш концентрацией аутрича по категориям; снизить число повторов с двух до одного для категорийных промптов; сократить состав движков, начиная с Claude в стандартном аудите, затем ChatGPT, но не Gemini; ограничить расширенный тариф одной дополнительной локалью; перевести извлечение на пакетный режим вне пикового времени.

Аутрич дарит тот же Free snapshot и стоит $0,06–0,08 на домен в пачке, пачка из 40 доменов — $2,5–3: категорийные ответы общие для всей пачки. До задачи T-71 аутрич запускается как обычные тизеры по одному домену — $0,07–0,17. При извлечении один раз на категорийный ответ (T-69) дешевеют и платные аудиты при попадании в кэш: стандартный — с $2,37 до $2,08, расширенный — с $4,24 до $3,76. Расчёт — в GEOTRACK_UNIT_ECONOMICS_CHECK_RU.md, версия 1.4.

Месяц на объёмах гейта G3 (300 тизеров, 30 платных аудитов, 30% из них в локалях ro-MD, ru-MD и ro-RO, 80 доменов аутрича): тизеры около $32, платные аудиты $125–150 в зависимости от бесплатной квоты Gemini, аутрич около $6, эталонные прогоны около $129, SerpAPI $75 по плану, инфраструктура $150–250. Итого $517–642 в месяц. Роудмэп закладывал $400–800. Экономика единицы не является риском фазы 1; риск — объём продаж.

### 12.3 Конкурентность и лимиты

Конкурентность задаётся ключом провайдера, а не глобально: ограничение OpenAI не должно замедлять Perplexity. Стартовые значения ставятся вдвое ниже документированных лимитов и поднимаются по факту. Очередь имеет ограниченную глубину, счётчик живёт в Redis на стороне API создания аудита: при переполнении новые бесплатные тизеры получают вежливый отказ с предложением вернуться позже, а платные аудиты ставятся в очередь — это лучше, чем деградация для всех.

---

## 13. План работ на десять недель

Задачи нарезаны размером в два-три часа работы агента: одна задача — один шаг пайплайна, один экран или один контракт. Идентификатор задачи совпадает с именем ветки и файла спецификации. Столбец «зависит от» задаёт порядок жёстче, чем номер недели: задачу нельзя начинать, пока её зависимости не слиты в `main`.

### 13.0 Неделя 0: техническая подготовка

Три дня подготовки из роудмэпа имеют техническую часть, которая блокирует задачу T-01 и делается руками продакта, а не агентом: создать организацию и проекты в Vercel, Railway, Supabase с европейским регионом, Upstash и Inngest; получить ключи провайдеров моделей и движков и выставить лимиты расходов на их стороне; опубликовать черновые условия, политику конфиденциальности и политику возвратов на домене и подать заявку в Paddle; зарегистрировать домен и почтовый домен с записями для отправки писем; завести репозиторий, положить в него документы проекта, этот план, CLAUDE.md и шестнадцать записей ADR.

Правило на неделю 0: ключи сразу создаются парами, отдельно для разработки и для продакшена. Разделить их позже, когда один ключ уже прописан в трёх местах, заметно дороже.

### 13.1 Недели 1–4, до гейта G1

| Номер | Задача | Зависит от | Критерий приёмки |
|---|---|---|---|
| T-00 | Деплой веб и воркера, проверка конфигурации при старте, Sentry, PostHog, Langfuse | T-01 | Пустой деплой в preview и прод, сервис не стартует с неполной конфигурацией, ошибка видна в Sentry |
| T-01 | Монорепозиторий, pnpm workspaces, CI, линтер, правило границ пакетов и импортов сети | — | Пустая сборка зелёная, нарушение границы пакета ломает CI |
| T-02 | Схема данных v1 с полями раздела 4.6, миграции, сиды, проверка Zod и Drizzle | T-01 | Миграции применяются к чистой базе, сид-набор из трёх сайтов загружается |
| T-03 | Контракты: Step, StepContext, ArtifactRef, UsageRecord, реестр движков, профили аудита | T-02 | Типы экспортируются из core, есть тест на идемпотентный ключ и чтение профиля |
| T-04 | Учёт стоимости, прайс-таблицы провайдеров, потолки профилей | T-03 | Стоимость вызова считается, мягкий и жёсткий потолок срабатывают в тесте |
| T-05 | Переходы статусов, оркестратор Inngest, две заглушки шагов, лента событий с `seq` | T-03, T-04 | Пустой аудит проходит от события до записи; каждый запрещённый переход покрыт тестом; таймауты ожиданий работают на фикстурном времени |
| T-06 | Опрос ленты прогресса и токен прогресса | T-05 | Перезагрузка и обрыв любой длины восстанавливают ленту без пропусков и дублей |
| T-07 | Адаптер модели Gemini Flash, адаптер движка Perplexity, фикстуры, контрактный набор | T-03 | Общий контрактный набор зелёный, живой вызов записан в фикстуру |
| T-08a | Модуль safe-fetch: резолв, закрепление адреса, ручные редиректы, лимиты | T-03 | Все случаи раздела 9.2 для undici покрыты тестами |
| T-08b | Фабрика контекстов Playwright, проверка изоляции при старте воркера | T-08a | Все случаи раздела 9.2 для Playwright покрыты тестами, воркер не стартует при доступной приватной сети |
| T-08c | Краулер: глубина по профилю, robots, паузы | T-08a, T-08b | Снимок сайта в Storage, метрики страниц в базе |
| T-09 | Минимальная админка: список аудитов по статусам, артефакты, стоимость, перезапуск шага | T-05 | Аудит открывается, артефакт скачивается, перезапуск шага пишется в журнал, `needs_attention` — первый экран |
| T-10 | Паспорт продукта | T-07, T-08c | Схема вывода валидна на пяти эталонных сайтах |
| T-11 | Таксономия категорий, 200 записей с ведущими игроками категории | — | Файл в config, тест на уникальность и полноту полей |
| T-12 | Детекция категории с уверенностью, окно выбора, автовыбор по таймауту | T-10, T-11 | Точность 85% на 20 размеченных сайтах, окно реже 40%, автовыбор помечается |
| T-13 | Детекция конкурентов без движков и SerpAPI | T-10 | Семь из десяти конкурентов релевантны на 20 сайтах |
| T-14a | Генерация канонических наборов для 200 категорий, локали en и ro | T-11 | 400 файлов в config, 40 промптов в каждом, нет дублей, выборочная ручная проверка пройдена |
| T-14b | Генератор брендовых промптов, сборка набора по профилю | T-12, T-13, T-14a | 12, 30 и 50 промптов по профилям, брендовые помечены |
| T-15 | Технический чек B1–B6, число запросов B4 из профиля | T-08c | Два прогона подряд дают одинаковый результат на пяти сайтах |
| T-16 | Правила блокеров и скор столпа B | T-15 | Каждое правило покрыто модульным тестом, включая отключение при неизмеренном критерии |
| T-17 | Кэш категорий по идентификаторам | T-02, T-14b | Второй сайт категории дешевле первого, брендовые промпты не кэшируются, повторы дозапрашиваются |
| T-18 | Шаг опроса движков: конкурентность, повторы, потолки профиля | T-07, T-17 | Отказ одного движка даёт partial, жёсткий потолок останавливает опрос |
| T-19 | Извлечение упоминаний, позиций, источников | T-18 | Метрики выше порогов раздела 5.4 на 30 размеченных ответах |
| T-20 | Скоринг столпов A и B, доверительный интервал по формуле 4.5 | T-16, T-19 | Неизмеренный критерий исключается из знаменателя, интервал при одном и двух повторах, тесты формул зелёные |
| T-21 | Лендинг и форма ввода URL, Turnstile | T-01 | Форма создаёт аудит, бот без токена получает отказ |
| T-22 | Экран прогресса из статуса и окно выбора категории | T-06, T-12 | Выбор пользователя продолжает пайплайн без перезапуска; экран рисует каждый статус |
| T-23 | Страница результата тизера, три шаблонные находки | T-20 | Скор и находки совпадают с админкой |
| T-24 | Шареабельная карточка результата | T-23 | Карточка генерируется и корректно разворачивается в соцсетях |
| T-25 | Email-гейт: ввод продолжает пайплайн, magic link для повторного доступа, письмо результата | T-23 | Брендовые промпты запускаются после ввода email без подтверждения |
| T-26 | Лимиты по домену и IP, глубина очереди, флаг `is_internal`, румынская локаль интерфейса | T-21 | Второй тизер на домен в 30 дней отклоняется, внутренний прогон лимит обходит, локаль переключается |

### 13.2 Недели 5–8, до гейта G2

Задачи T-59–T-67 внесены версией 1.2. Они не переписывают задачи, которые могут быть уже слиты (T-02, T-11, T-14a, T-17): расширение идёт новыми миграциями, новой версией профилей и новой версией наборов промптов. Это добавляет около девяти задач в самый плотный отрезок плана — ёмкость недель 5–8 становится главным риском версии, порядок сокращения — в разделе 13.5.

| Номер | Задача | Зависит от | Критерий приёмки |
|---|---|---|---|
| T-27 | Адаптер ChatGPT, Responses API с веб-поиском на модели среднего класса | T-07 | Общий контрактный набор зелёный, условия хранения проверены (ADR-017) |
| T-28 | Адаптер Gemini с grounding | T-07 | Общий контрактный набор зелёный, учёт бесплатной квоты в прайс-таблице |
| T-78 | Адаптер Claude: Messages API, Haiku 4.5, веб-поиск с `max_uses: 1`, учёт платы за поиск, ADR-023 | T-07 | Общий контрактный набор зелёный; в каждом ответе не больше одного поиска; условия хранения ответов Anthropic проверены вместе с ADR-017 |
| T-29 | Полный прогон стандартного профиля: 30 промптов, четыре движка, два повтора | T-18, T-27, T-28, T-78 | Аудит укладывается в бюджет времени раздела 12.1; замер токенов на вызов по движкам |
| T-30 | Критерий A5, точность описания продукта | T-19 | Совпадение с разметкой 0,85 на 50 утверждениях |
| T-31 | Критерий A6, тональность; полные золотые наборы упоминаний и источников | T-19 | Совпадение с разметкой 0,80 на 100 ответах; наборы T-19 добраны до 100 |
| T-32 | Анализ источников цитирования, карта источников | T-29 | Карта строится для 100% аудитов, точность домена 0,95 |
| T-33 | Контент-анализ C1–C6 на десяти страницах | T-08c, T-32 | Баллы и артефакты на пяти эталонных сайтах |
| T-34 | Сравнение с цитируемыми страницами конкурентов | T-32, T-33 | В находке есть ссылка на конкретную цитируемую страницу |
| T-35 | Скоринг столпа C | T-33 | Модульные тесты критериев зелёные |
| T-36 | Столп D: критерии D1, D2, D4 через SerpAPI | T-13 | Недоступность API помечает критерий неизмеренным |
| T-37 | Столп E: доступность через axe-core, барьеры | T-08b | Отчёт axe сохраняется как артефакт |
| T-38 | Столп E: машиночитаемые данные и манифесты | T-08c | Критерии E4 и E5 дают баллы |
| T-39 | Столп E: сценарии «найти цену» и «начать регистрацию» | T-37 | Таймаут сценария трактуется как результат, скриншоты сохранены |
| T-40 | Скоринг столпов D и E, общий скор и полосы | T-35, T-36, T-38, T-39 | Пересчёт исторического аудита новой версией конфигурации |
| T-41 | Генератор находок семи полей, приоритизация по формуле 4.5 | T-40 | До 15 находок, половина не требует ручной правки |
| T-42 | Корзина исправлений с прогнозом скора по формуле 4.5 | T-41 | Прогноз воспроизводим и объясним по каждой услуге |
| T-43 | Веб-отчёт по токену, версия методологии на момент оплаты | T-41 | Чужой токен получает отказ, индексация запрещена |
| T-44 | PDF отложенной сборкой через внутренний маршрут | T-43 | Ошибка PDF не блокирует веб-отчёт, маршрут недоступен без сервисного секрета |
| T-45 | Оплата через Paddle: вебхуки, создание платного аудита из тизера, письмо подтверждения, возврат из админки, рантбук | T-43 | Дубль события не создаёт второй аудит, цена берётся с сервера, возврат пишется в журнал |
| T-46 | Три письма: результат, готовность, письмо через сутки; письмо «отчёт дополнен» | T-25, T-47 | Письма приходят на двух языках, письмо готовности уходит только после одобрения |
| T-47 | Админка: ручная проверка отчёта, окно 24 часа, автоотправка чистых аудитов | T-41 | Отчёт можно задержать; чистый аудит уходит сам через 24 часа, аудит с пометками ждёт |
| T-59 | Справочник локалей «язык плюс рынок» (en, ro-MD, ru-MD, ro-RO); миграция полей `prompt_locale`, `extra_locales`, `report_lang`; выбор локали и языка отчёта в форме; автоопределение по языку сайта и домену; фич-флаг | T-02, T-21, T-26 | Локаль промптов и язык отчёта выбираются раздельно; ru-MD не включает русский интерфейс; ключ кэша различает ro-MD и ro-RO |
| T-60 | Персоны запроса в таксономии, две-три на категорию; выбор целевой персоны в `category.detect` | T-11, T-12 | Персоны есть у всех 200 категорий; на 20 размеченных сайтах целевая персона верна в 80% случаев |
| T-61 | Канонические наборы v2: четыре локали, персонные варианты, генерация на языке локали | T-14a, T-59, T-60 | 800 наборов по 40 промптов; треть discovery и problem-led с `persona_id`; промпт задачи генерации на языке локали; продакт проверил по 10 наборов в ro-MD, ru-MD и ro-RO |
| T-62 | Варианты написания бренда: транслитерация латиница–кириллица, варианты из паспорта, правка в админке | T-10, T-19 | Бренд, написанный кириллицей, находится в ответах ru-MD; правка вариантов пересчитывает извлечение без повторного опроса |
| T-63 | Режим без поиска: `search` в `AskOptions`, возможности в реестре, режим в `engine_runs` и в ключе кэша, индикатор вне формулы скора | T-17, T-27, T-28 | Общий контрактный набор зелёный в обоих режимах; режимы не смешиваются в кэше; индикатор не меняет скор |
| T-64 | Шаг `prompts.client`: вопросы покупателей, FAQ и отзывы с сайта, сниппеты отзывов через SerpAPI, генерация на языке локали | T-10, T-14b, T-59 | 6 и 10 промптов по профилям, у каждого источник; в кэш не попадают; без источников — пометка «модельные» |
| T-65 | Поля на странице выбора тарифа аудита перед оплатой: до пяти вопросов покупателей и дополнительные локали для расширенного тарифа | T-23, T-59 | Значения сохраняются в тизере до перехода к оплате и читаются платным аудитом |
| T-66 | Срезы отчёта и находки разрывов по разделу 7.4, маршрутизация к услугам | T-41, T-63, T-64 | Срез показывается от шести промптов; каждая находка разрыва ведёт к услуге из каталога; порог читается из конфигурации |
| T-67 | Дополнительные локали расширенного профиля: сокращённый набор, столп A по локали, потолки с надбавкой | T-29, T-59, T-61 | Аудит с тремя локалями укладывается в бюджет времени и потолок профиля; общий скор считается по основной локали |

### 13.3 Недели 9–10, до гейта G3

| Номер | Задача | Зависит от | Критерий приёмки |
|---|---|---|---|
| T-48 | Харнесс калибровки: пакетный прогон 50 сайтов с обходом кэша, один на расширенном профиле | T-40 | Отчёт о разбросе скора, времени и стоимости по шагам |
| T-49 | Бенчмарки категорий в отчёте | T-48 | Медиана категории показывается при 20 аудитах и более, внутренние аудиты исключены |
| T-50 | Румынская версия отчёта и писем; проверка аудита в локалях ro-MD, ru-MD и ro-RO | T-46, T-61 | Проверка на пяти сайтах в каждой из трёх локалей пройдена |
| T-51 | Страница методологии, финальные юридические страницы, удаление данных | T-43 | Удаление по запросу работает и покрыто тестом |
| T-52 | Дашборд метрик воронки, себестоимости и попаданий в кэш | T-45 | Себестоимость аудита и тизера видна за любой период |
| T-53 | Исправления по обратной связи беты | T-52 | Список из беты закрыт или перенесён в долг явно |
| T-54 | Демо-сценарий и защита от нагрузки в демо | T-52 | Живой аудит за две минуты на подготовленном сайте |
| T-55 | Ночные харнессы: eval, живые контракты, прогон эталонных сайтов на профиле тизера, еженедельный на стандартном | T-29 | Таблица истории заполняется, алерт при сдвиге больше 0,5 балла |
| T-56 | Очистка артефактов по срокам хранения | T-02 | Артефакты тизера старше 30 дней удалены, платного — сохранены |
| T-57 | Рантбуки: пять оставшихся сценариев | T-55 | Шесть файлов в docs/runbooks |
| T-58 | Проверка восстановления бэкапа | T-02 | Восстановление в отдельный проект выполнено вручную, время записано |
| T-68 | Золотые подмножества ro и ru для упоминаний и тональности; два эталонных сайта локалей ro-MD и ru-MD в ночном прогоне | T-31, T-62 | Пороги раздела 5.4 выполняются по каждому языку |

Задачи T-69–T-76 внесены версиями 1.3–1.5. T-69 и T-70 удешевляют все аудиты и идут на неделях 5–6; задачи аутрича — на неделях 8–9. До их слияния аутрич запускается как обычные тизеры по одному домену.

| Номер | Задача | Зависит от | Критерий приёмки |
|---|---|---|---|
| T-69 | Извлечение один раз на категорийный ответ: сущности в записи кэша, сопоставление брендов кодом, версия извлечения | T-17, T-19 | Аудит при попадании в кэш не вызывает модель для категорийных ответов; F-мера упоминаний на золотом наборе не ниже порога раздела 5.4; смена версии извлечения переизвлекает запись |
| T-70 | Кэш внешнего следа по домену бренда, 14 дней | T-36 | Второй аудит, где бренд — конкурент, не делает запросов SerpAPI по этому бренду |
| T-71 | Профиль аутрича: пачка, общий прогон категории, снапшот на профиле тизера для каждого домена, `source = outreach`, переиспользование свежего снапшота | T-69, T-17, T-25 | Пачка из 40 доменов стоит не больше $4; снапшот аутрича совпадает с входящим по составу; повторный ввод домена на лендинге открывает готовый результат |
| T-72 | Вход по ссылке аутрича: экран результата тизера с баннером «Prepared for {Company}», тот же email-гейт, метрики воронки по источнику | T-23, T-25, T-71 | Открытие ссылки не запускает прогон; после email виден полный снапшот; воронка показывает источник outreach отдельной строкой |
| T-73 | Пачки аутрича в админке: загрузка списка доменов, запуск по расписанию, черновик письма с цифрами снапшота | T-09, T-71 | Пачка создаётся из списка; письмо подставляет число упоминаний клиента и конкурента из 10 и главную находку |
| T-74 | Пакетный режим провайдеров для извлечения в эталонных прогонах и калибровке, где он поддерживается | T-69 | Проверка поддержки записана в ADR; поддерживаемые вызовы идут пакетом, остальные — обычным режимом |
| T-75 | Бюджеты аутрича в ядре: `outreach_settings`, поля бюджета пачки, резерв в месячном лимите при запуске, остановка пачки по бюджету, жёсткие пределы из `config`, ADR-022 | T-04, T-71 | Тесты до кода: оценка пачки, резерв и возврат остатка, две пачки подряд не превышают месяц, отказ при значении выше предела, домены после исчерпания бюджета получают статус «пропущен: бюджет» |
| T-76 | Раздел «Аутрич, бюджет» в админке: значения по умолчанию, месячный лимит, оценка пачки до запуска, журнал изменений, уведомления | T-73, T-75 | Запуск неактивен, если оценка выше бюджета, и админка предлагает наибольшее число доменов; каждое изменение в журнале |

### 13.4 Технические критерии гейтов

Продуктовые критерии гейтов заданы роудмэпом. Здесь — технические, которые проверяются до того, как считать гейт пройденным.

| Гейт | Технические критерии |
|---|---|
| G1, 4 октября | Тизер за 90 секунд машинного времени в 95% прогонов; стоимость ниже $0,15 при попадании в кэш и ниже $0,25 при промахе; ноль аудитов с пустым столпом B; точность категории 85%; доля неуспешных прогонов ниже 5%; проверки раздела 9.2 покрыты тестами; все статусы проходятся на живом тизере |
| G2, 1 ноября | Полный аудит за шесть минут в 95% прогонов и дешевле $6 при промахе кэша; повтор на том же сайте с обходом кэша даёт скор в пределах интервала в девяти случаях из десяти; дубль вебхука не создаёт второй аудит; отчёт по чужому токену недоступен; возврат из админки работает на тестовом платеже; аудит в локали ru-MD находит бренд, написанный кириллицей; режимы и локали не смешиваются в кэше; чек-лист приложения Е пройден |
| G3, 15 ноября | Прогон 50 сайтов завершён, разброс в пределах интервала; дашборд себестоимости работает; шесть рантбуков написаны; критических уязвимостей в зависимостях нет; восстановление бэкапа проверено вручную; очистка артефактов работает; пороги извлечения выполняются на ro и ru; пачка аутрича из 40 доменов стоит не больше $4 |

### 13.5 Резерв и правила сокращения скоупа

Резерв — две недели, как в архитектурном документе, и он расходуется на калибровку и внешние сервисы, а не на новые функции. Правило роудмэпа «двигается скоуп, а не гейт» раскрывается в технический порядок отказа:

1. Первым сокращается число повторов категорийных промптов с двух до одного: интервал считается по константе из конфигурации, функциональность не теряется.
2. Вторым — пакетный режим провайдеров (T-74): извлечение в эталонных прогонах идёт обычным режимом, это немного дороже.
3. Третьим — дополнительные локали расширенного тарифа (T-67): расширенный продаётся с одной локалью, страница тарифа говорит об этом прямо.
4. Четвёртым — срез по персонам в отчёте (часть T-66): персонные промпты остаются в наборе, срез не показывается.
5. Пятым — состав движков в платном аудите: сначала Claude уходит из стандартного аудита и остаётся в расширенном, затем ChatGPT — до двух движков; Gemini в бесплатной квоте дешевле всех. Методология допускает, отчёт указывает состав честно. Прогон без поиска без ChatGPT идёт на Gemini.
6. Шестым — критерий E1, скриптовые сценарии: столп E считается по E2–E5, блокер по E1 отключается конфигурацией.
7. Седьмым — румынская локаль отчёта, при сохранении локали интерфейса.

Что не сокращается никогда: тесты детерминированного ядра, контур безопасности раздела 9, учёт стоимости и бюджеты аутрича (T-75, T-76), ручная проверка отчёта перед отправкой в пределах окна 24 часа, промпты на языке локали — если набора для локали нет, аудит в ней не запускается, а не идёт на переводе.

---

## 14. Технические риски

| Риск | Сигнал | Действие |
|---|---|---|
| Изменение API или цен движка | Падение ночного теста живых контрактов, рост стоимости аудита | Переключить движок фич-флагом, обновить адаптер, пересчитать бюджет |
| Удвоение цены Gemini Flash с 1 января 2027 | Календарь | Пересчёт себестоимости в декабре, сравнение с Flash-Lite для извлечения |
| Модель среднего класса в адаптере ChatGPT расходится с интерфейсом ChatGPT | Замер недели 5 на пяти эталонных сайтах | ADR о переходе на флагман с пересмотром потолков и цены |
| Условия провайдеров запрещают хранение ответов 12 месяцев | Проверка до T-27 и T-28 | Сокращение срока для движка, агрегаты только из извлечённых фактов |
| Качество извлечения ниже порога после смены модели | Ночной eval ниже порога раздела 5.4 | Откат модели в конфигурации, разбор расхождений по таблице примеров |
| Дрейф движков меняет скор без наших изменений | Скор эталонного сайта сдвинулся больше чем на 0,5 балла | Зафиксировать в истории, проверить состав источников, сообщить клиентам при ре-аудите |
| Себестоимость не сходится к потолку профиля | Дашборд себестоимости на неделе 5 | Рычаги раздела 12.2 по порядку |
| Технический долг от разработки агентом | Регрессии на еженедельном прогоне чаще раза в две недели | Неделя стабилизации, усиление тестов, консультация инженера |
| Отказ платёжного провайдера или задержка одобрения | Нет одобрения Paddle к неделе 6 | Lemon Squeezy как запасной, ручные счета для первых пяти клиентов |
| Инъекция из контента сайта влияет на результат | Сработали логи подозрительных образцов, аномальный скор | Разбор аудита в админке, ужесточение схемы вывода, слои раздела 9.3 |
| Злоупотребление бесплатным тизером | Рост числа тизеров без роста email | Ужесточить лимит по домену, поднять порог Turnstile, включить дневной потолок |
| Оплаченные аудиты копятся в `needs_attention` | Больше двух за неделю | Разбор причин по рантбуку, усиление деградации шага, консультация инженера |
| Один человек недоступен во время инцидента | Инцидент длится дольше суток | Рантбуки, автоматическая деградация, автоотправка чистых отчётов, отключение бесплатного потока как аварийный режим |
| Не хватает ёмкости недель 5–8 после добавления T-59–T-67 | Меньше половины задач закрыто две недели подряд | Порядок сокращения раздела 13.5, резерв двух недель |
| Кэш локалей ro-MD, ru-MD, ro-RO заполняется медленно | Доля попаданий в кэш по этим локалям ниже 30% на неделе 9 | Концентрация аутрича по категориям в этих локалях, планирование себестоимости по промаху |
| Извлечение на ro и ru хуже, чем на en | Eval T-68 ниже порога | Отдельная модель для этих языков через роутер, расширение вариантов написания бренда |
| Движок не принимает регион пользователя | Проверка возможностей адаптера в T-63 | Пометка в методике отчёта, страна рынка в тексте локальных промптов |
| Клиентских источников нет у большинства сайтов | Доля «модельных» клиентских промптов выше 50% | Сделать поле вопросов покупателей заметнее, добавить источник — публичные обсуждения продукта |
| Free snapshot слабее полного отчёта как подарок в холодном письме | Меньше трёх ответов на 80 писем | Усилить письмо и главную находку, а не подарок; если не помогает — обсуждать расширение Free snapshot для всех каналов сразу |
| Общее извлечение пропускает малоизвестный бренд | Расхождение упоминаний T-69 с золотым набором | Страховка сопоставлением по тексту ответа, пометка для проверки, откат версии извлечения |
| Бюджет пачки выставлен слишком низко | Больше 10% доменов пачки пропущены по бюджету | Поднять бюджет по умолчанию в админке |
| Фактическая стоимость снапшота выше оценки | Средняя стоимость домена в пачке выше $0,10 | Пересчитать значения по умолчанию, проверить потолки профиля и долю попаданий в кэш |

---

## 15. Выводы

Технический план держится на пяти решениях. Первое: качество переносится из человеческого ревью в контракты, тесты и автоматические гейты, потому что читать весь код будет некому. Второе: код делится на три класса с разной дисциплиной, и попытка применить строгий порядок «тест до кода» к вероятностной части приведёт либо к самообману, либо к остановке работы — там работают золотые наборы и пороги. Третье: сначала сквозной скелет, потом вертикальные срезы по столпам, потому что для одного разработчика ошибка в контракте, найденная на шестой неделе, дороже недели, потраченной на пустой аудит. Четвёртое: безопасность строится вокруг трёх особенностей продукта — переход по произвольному адресу, деньги за каждый запрос и чужой текст в контексте модели, и главная защита от последней угрозы архитектурная: модель извлекает факты, баллы считает код. Пятое, добавленное после ревью: объём аудита задают профили, состояние — таблица переходов, а экономика держится на канонических промптах категорий; всё три — данные в `config` и `core`, а не условия в коде шагов. Шестое, добавленное в версии 1.2: промпт — измерительный прибор, и он настраивается под рынок. Локаль — пара «язык плюс рынок», наборы пишутся на её языке, клиентский слой приносит слова покупателя, персоны запроса и два режима опроса дают срезы, из которых собираются находки разрывов. Седьмое, добавленное в версиях 1.3–1.5: категорийный ответ разбирается один раз для всех аудитов, а аутрич — это тот же Free snapshot, подготовленный пачкой заранее. Одна точка входа в воронку, одна цена и одни метрики для обоих каналов.

Следующий шаг версий 1.2–1.5 — записать ADR-018–ADR-023 по приложению Г, обновить CLAUDE.md по приложению Д и завести спецификации T-59–T-76 и T-78. Работа до гейта G1 не меняется.

---

## Приложение А. Шаблон спецификации задачи

```markdown
# T-XX. Название задачи

Статус: черновик | согласовано | выполнено
Неделя: N
Источник: документ и раздел, откуда взято требование

## Задача
Одно-два предложения. Что появится в продукте после этой задачи.

## Входы и выходы
Типы из packages/core. Если типа ещё нет, он вводится этой задачей.
Какие параметры читаются из профиля аудита.

## Критерий приёмки
Проверяемые утверждения списком. Каждое должно быть выразимо тестом
или наблюдаемо на живом сайте.

## Тесты, которые должны появиться
Перечень. Для детерминированного ядра пишутся до кода.

## Границы
Файлы и папки, которые задача трогает. Всё остальное не меняется.

## Что не делаем
Явный список, чтобы задача не расползлась.

## Риски и открытые вопросы
```

## Приложение Б. Пример спецификации: T-15, технический чек столпа B

```markdown
# T-15. Технический чек столпа B (критерии B1–B6)

Статус: согласовано
Неделя: 3
Источник: GEOTRACK_AUDIT_METHODOLOGY_RU.md, столп B

## Задача
Шаг tech.check собирает факты для шести критериев столпа B и отдаёт их
скорингу. Баллы в этой задаче не считаются.

## Входы и выходы
Вход: домен, robots.txt и sitemap из снимка краулера, список ключевых страниц,
число запросов SerpAPI для B4 из профиля (2 для тизера, 11 для платного).
Выход: TechCheckFacts — по каждому критерию структура фактов и признак
измеренности. Артефакты: ответы на запросы с разными user-agent, снимок
страницы без JavaScript.

## Критерий приёмки
- Для каждого из шести критериев возвращается структура фактов либо признак
  «не измерен» с причиной.
- Проверка robots.txt различает восемь ботов из методологии по отдельности.
- Запрос с user-agent AI-бота выполняется не чаще одного в секунду и
  останавливается при получении 429.
- Рендер без JavaScript сравнивается с обычным рендером по объёму основного
  текста, а не по длине HTML.
- Индексация в Bing проверяется через поисковый API числом запросов из профиля;
  недоступность API даёт «не измерен», а не ноль.
- Два прогона подряд на пяти эталонных сайтах дают одинаковый результат.

## Тесты, которые должны появиться
- Разбор robots.txt: разрешение, запрет, отсутствие файла, файл с ошибками,
  правило для конкретного бота поверх общего.
- Сравнение рендеров на фикстуре страницы, которая без JavaScript пуста.
- Соблюдение паузы между запросами к домену.
- Недоступность поискового API помечает B4 неизмеренным.
- Тизерный профиль делает ровно два запроса SerpAPI.

## Границы
apps/worker/steps/tech-check.ts, packages/core/checks/robots.ts, фикстуры
fixtures/sites.

## Что не делаем
Не считаем баллы, не формируем находки, не проверяем CDN-блокировки по
подписи ответа — это отдельная задача.

## Риски
Часть сайтов отдаёт разный robots.txt по географии запроса; фиксируем факт
и указываем регион проверки в артефакте.
```

## Приложение В. Пример спецификации: T-20, скоринг столпов A и B

```markdown
# T-20. Скоринг столпов A и B и доверительный интервал

Статус: согласовано
Неделя: 3
Источник: GEOTRACK_AUDIT_METHODOLOGY_RU.md, разделы о весах и блокерах;
GEOTRACK_TECH_PLAN_MVP_RU.md, раздел 4.5

## Задача
Чистая функция, которая превращает структурированные факты и версию
конфигурации в баллы по критериям, баллы по столпам и общий скор с
интервалом.

## Входы и выходы
Вход: ExtractedFacts, TechCheckFacts, methodology.v1.json, число повторов
из профиля.
Выход: ScoreResult — баллы по критериям, по столпам, общий скор, интервал,
список неизмеренных критериев, применённые блокеры.

## Критерий приёмки
- Функция не обращается к сети и базе.
- Неизмеренный критерий исключается из знаменателя столпа и расширяет
  интервал по формуле раздела 4.5, а не считается нулём.
- Каждое правило блокера применяется ровно один раз, только при измеренных
  опорных критериях, и фиксируется в выводе.
- Интервал при двух повторах считается по разбросу, при одном — по константе
  из конфигурации.
- Общий скор приводится к шкале 0–10 с одним знаком и попадает в правильную
  полосу интерпретации.
- Аудит, сохранённый ранее, пересчитывается другой версией конфигурации без
  изменения кода.

## Тесты, которые должны появиться
Пишутся до кода: сумма весов равна единице; граничные значения полос;
каждое правило блокера отдельным тестом и тест его отключения; поведение при
одном, двух и всех неизмеренных критериях столпа; интервал при одном и двух
повторах; значение вне диапазона обрезается и логируется; пересчёт
исторического аудита.

## Границы
packages/core/scoring, packages/config/methodology.v1.json.

## Что не делаем
Не формируем находки, не считаем прогноз для корзины, не трогаем столпы C, D, E.
```

## Приложение Г. Шаблон ADR и решения недели 0

Шаблон:

```markdown
# ADR-XXX. Заголовок решения

Дата. Статус: предложено | принято | заменено ADR-YYY.

## Контекст
Что заставило принимать решение.

## Решение
Одно предложение.

## Причины
Два-три пункта.

## Последствия
Что станет проще, что сложнее, что придётся переделать, если решение изменится.
```

Шестнадцать решений, которые фиксируются до первой строки кода. Десять исходных: оркестратор Inngest и условия перехода на Temporal; Supabase как база, хранилище и аутентификация, регион Европа; контракт шага и правило идемпотентности; роутер моделей и выбор основной малой модели; формат и версионирование конфигурации весов; схема токена отчёта и модель доступа; merchant of record Paddle с запасным вариантом; политика фикстур и запрет сетевых вызовов в тестах; локализация через next-intl и место хранения промптов по локалям; границы пакетов и правило зависимостей. Шесть по итогам ревью, полный текст в GEOTRACK_TECH_PLAN_DECISIONS_RU.md: ADR-011 канонические промпты категорий и ключ кэша; ADR-012 жизненный цикл аудита; ADR-013 профили аудита и переход из тизера; ADR-014 доставка платного отчёта при сбоях; ADR-015 защита исходящих запросов воркера; ADR-016 лента прогресса через опрос. Семнадцатое, ADR-017 об условиях хранения ответов провайдеров, пишется до T-27. Три решения версии 1.2: ADR-018 — локали как пара «язык плюс рынок», генерация на языке локали и запрет перевода промптов; ADR-019 — режим без поиска и индикатор вне скора; ADR-020 — клиентский слой промптов и персоны запроса в таксономии. Решение версии 1.7: ADR-023 — Claude как четвёртый движок платных профилей: модель, ограничение поиска, условия хранения ответов. Решение версий 1.4–1.5: ADR-022 — бюджеты аутрича: рабочие значения в базе и админке, жёсткие пределы в `config`, проверка и резерв при запуске пачки. Решение версий 1.3 и 1.5: ADR-021 — аутрич как Free snapshot, подготовленный пачкой, с `source = outreach` и одной точкой входа в воронку; извлечение один раз на категорийный ответ фиксируется дополнением к ADR-011.

## Приложение Д. Скелет CLAUDE.md

```markdown
# GeoTrack — соглашения проекта

## Что это
Одно предложение о продукте и ссылки на документы в docs/product.

## Карта репозитория
apps/web, apps/worker, packages/core, packages/config, packages/db,
packages/ui, docs, fixtures — по строке на каждый.

## Команды
Установка, запуск, тесты, миграции, запись фикстур, генерация наборов
промптов, прогон eval.

## Инварианты: не менять без ADR
Контракт шага и идемпотентность; таблица переходов статусов; профили аудита;
интерфейсы адаптеров; схема данных; конфигурация весов и интервала; границы
пакетов; модель доступа к отчёту и админке; safe-fetch и фабрика Playwright;
список провайдеров; справочник локалей.

## Запреты
Не удалять и не отключать тесты. Не ходить в сеть из тестов. Не выходить в
сеть из воркера мимо safe-fetch и фабрики Playwright. Не проверять тип аудита
условиями — читать профиль. Не менять статус мимо transition. Не добавлять
зависимости без явного упоминания в описании PR. Не менять схему данных
внутри задачи про интерфейс. Не подставлять секреты в код и логи.
Не менять промпт или модель без прогона eval. Не переводить промпты между
локалями — генерировать на языке локали. Не класть брендовые и клиентские
промпты в кэш. Не добавлять Meta AI в реестр движков.

## Порядок работы
Спецификация в docs/specs, вопросы до кода, тесты, реализация, ревью
отдельной сессией, PR по шаблону.

## Текущее состояние
Неделя плана, что уже готово, что в работе, куда смотреть в первую очередь.
```

Файл держится в пределах двух страниц и обновляется в конце каждой недели.

## Приложение Е. Чек-лист безопасности перед внешним событием

Проходится перед первым публичным тизером, включением оплаты и стартом беты.

- Модуль safe-fetch и фабрика Playwright покрыты тестами, включая редирект на приватный адрес и IP-литерал; линтер запрещает импорт сети мимо них.
- Проверка изоляции воркера при старте проходит в проде.
- Лимиты работают: один тизер на домен в 30 дней, лимит по IP, глубина очереди, Turnstile; внутренние прогоны помечены `is_internal`.
- Потолки стоимости включены на трёх уровнях, включая профили и лимиты на стороне провайдеров.
- Токен отчёта: 32 байта, в базе хэш, запрет индексации, строгая политика реферера; внутренний маршрут рендера недоступен без сервисного секрета.
- Подпись вебхука проверяется, обработка идемпотентна по событию и заказу, цена берётся с сервера, возврат пишется в журнал.
- RLS включён везде, политика по умолчанию запрещающая, админка закрыта ролью.
- Секретов нет в репозитории и в логах, сканер зелёный, ключи разделены по окружениям.
- Зависимости без критических уязвимостей, lock-файл зафиксирован.
- Очистка HTML перед передачей в модель работает, схемы вывода ограничены, аудиты с признаками инъекции не уходят автоматически.
- Бэкап сделан, восстановление проверено, функция удаления данных работает, очистка по срокам работает.
- Страница методологии описывает метод запросов с user-agent AI-ботов.

## Приложение Ж. Шаблон описания PR

```markdown
## Задача
T-XX, ссылка на спецификацию.

## Что сделано
Три-пять пунктов.

## Тесты
Какие добавлены. Для ядра — вывод падавших тестов до реализации.

## Инварианты
Не затронуты, либо ссылка на ADR.

## Новые зависимости
Нет, либо список с обоснованием каждой.

## Проверка вручную
Что и на каком сайте проверено, на каком профиле.

## Что осталось
Отложенное записано в docs/specs/debt.md.
```

## Приложение З. Что изменилось в версии 1.1

Изменения по итогам GEOTRACK_TECH_PLAN_CTO_REVIEW_RU.md, GEOTRACK_TECH_PLAN_DECISIONS_RU.md и GEOTRACK_UNIT_ECONOMICS_CHECK_RU.md:

- Добавлены разделы 4.7 «Профили аудита» и 4.8 «Жизненный цикл аудита»; в схему данных добавлены `status`, `profile_id`, `profile_version`, `parent_audit_id`, `is_internal`, `prompt_set_version`, `seq`.
- В раздел 4.5 добавлены формулы неизмеренных критериев, интервала, прогноза корзины и приоритета; правило версии методологии для платного клиента.
- Канонические наборы промптов категорий и ключ кэша по идентификаторам (7.3, 8.4); добавлен шаг `competitors.detect`; зафиксирована параллельность шагов (7.1).
- Правила доставки платного отчёта при сбоях и обещание «24 часа» (8.3, 12.1); модель среднего класса для адаптера ChatGPT (8.1).
- Раздел 9.2 переписан: safe-fetch, фабрика Playwright, изоляция воркера; уточнены токен как единственный механизм доступа, email-гейт без подтверждения, внутренний маршрут PDF (9.5); подтверждение оплаты и возврат (9.6).
- Лента прогресса через опрос (T-06); Playwright перенесён на слияние в `main` (5.2, 5.6); ночные прогоны на профиле тизера (8.5).
- Себестоимость пересчитана по ценам на 11 сентября 2026, потолки — по профилям, B4 в тизере — два запроса (12.2).
- Добавлены задачи T-00, T-08a–c, T-14a–b, T-55–T-58; уточнены критерии T-05, T-16, T-19, T-26, T-45–T-47, T-48; T-46 зависит от T-47.
- Устранены расхождения: время тизера, E5 в тизере, технический чек в бюджете времени, «в работе одна ветка», реестр движков вместо перечисления, число документов проекта.

## Приложение И. Что изменилось в версии 1.2

Изменения по решениям 24 сентября 2026 о методике промптов (концепция, версия 1.3, раздел 9.2):

- Локали — пара «язык плюс рынок»: en, ro-MD, ru-MD, ro-RO; промпты генерируются на языке локали, перевод запрещён; справочник локалей добавлен в инварианты (2.3, 3.6, 7.3, 8.2).
- Профили аудита v2: локали, три класса промптов, прогон без поиска, дополнительные локали расширенного тарифа, надбавки к потолкам и бюджету времени (4.7).
- Новый шаг `prompts.client`, всего 15 функций шагов; персона запроса в `category.detect`; варианты написания бренда в паспорте (7.1, 7.2, 7.3).
- Режим без поиска в адаптерах, в `engines.poll` и в ключе кэша; индикатор «знание без поиска» вне скора; правило скора при нескольких локалях (4.3, 4.5, 8.4).
- Раздел 7.4 о срезах отчёта и находках разрывов с маршрутизацией к услугам.
- Три новых золотых набора: упоминания и тональность на ro и ru, клиентские промпты (5.4).
- Себестоимость пересчитана оценочно: стандартный $3,60 и $1,87, расширенный $5,92 и $3,27 плюс $2,41 и $0,69 за локаль (12.2, по GEOTRACK_UNIT_ECONOMICS_CHECK_RU.md версии 1.1); бюджет времени дополнен (12.1).
- Исправлена стоимость эталонных прогонов: еженедельный прогон идёт с обходом кэша, это около $92 в месяц на семи сайтах, а не $30 (8.5, 12.2).
- Задачи T-59–T-68; T-50 расширена до трёх локалей; гейты G2 и G3 дополнены; порядок сокращения скоупа и технические риски обновлены (13, 14).
- ADR-018–ADR-020 и правила CLAUDE.md (приложения Г и Д).

## Приложение К. Что изменилось в версии 1.3

Изменения по расчёту себестоимости аутрича 25 сентября 2026 (GEOTRACK_UNIT_ECONOMICS_CHECK_RU.md, версия 1.2):

- Профиль аутрича с тремя этапами: прогон категории на пачку, предскан, полный отчёт по действию получателя (4.7).
- Статус `waiting_open` и правило запуска по действию, а не по открытию ссылки (4.8).
- Извлечение один раз на категорийный ответ с хранением сущностей в кэше (8.4); кэш внешнего следа по домену бренда (7.2).
- Стоимость аутрича и новая стоимость платных аудитов при попадании в кэш, месяц G3 с аутричем (12.2).
- Задачи T-69–T-74, критерий гейта G3, порядок сокращения, три новых риска, ADR-021 (13, 14, приложение Г).

## Приложение Л. Что изменилось в версии 1.4

- Раздел 4.10 «Бюджеты аутрича»: три уровня бюджета, настройка в админке, оценка до запуска, резервирование, поведение при нехватке.
- Статус `waiting_budget` (4.8); жёсткие пределы бюджета — в инвариантах (2.3).
- Задачи T-75–T-77, бюджеты — в списке того, что не сокращается; два новых риска; ADR-022 (13, 14, приложение Г).

## Приложение М. Что изменилось в версии 1.5

Решение 25 сентября 2026: письмо с аудитом по функционалу совпадает с Free snapshot, это одна точка входа в воронку для аутрича и лендинга.

- Профиль аутрича (4.7) — это профиль тизера, подготовленный пачкой заранее: общий прогон категории на Perplexity, снапшот для каждого домена при запуске, `source = outreach` вместо `is_internal`.
- Статусы `waiting_open` и `waiting_budget` и правило запуска по действию получателя убраны (4.8): при запуске тратится всё, открытие ссылки ничего не стоит.
- Бюджеты аутрича (4.10) упрощены: проверка при запуске, остановка пачки по бюджету; значения по умолчанию $0,10 на домен, $5 на пачку из 40, $20 в месяц.
- Стоимость аутрича — $0,06–0,08 на домен, месяц G3 — $441–566 (12.2).
- Задачи T-71, T-72, T-73, T-75, T-76 переписаны, T-77 исключена; T-74 касается только эталонных прогонов и калибровки; гейт G3, порядок сокращения, риски и ADR-021 обновлены.

## Приложение Н. Что изменилось в версии 1.6

- Реестр движков (4.3) и CLAUDE.md (приложение Д): Claude и Meta AI не используются для оценки видимости и в реестр не добавляются.
- Страница выбора тарифа аудита названа без номера экрана (4.7, T-65).

## Приложение О. Что изменилось в версии 1.7

- Claude Haiku 4.5 — четвёртый движок платных профилей, только с поиском, один поиск на вызов (4.3, 4.7, 8.1); Meta AI по-прежнему не используется.
- Задача T-78 — адаптер Claude и ADR-023; T-29 зависит от T-78; порядок сокращения начинается с Claude в стандартном аудите (13).
- Себестоимость: стандартный $2,37–4,90, расширенный $4,24–8,09 плюс $0,98–3,50 за локаль; надбавка к потолкам за локаль — $4 и $5; месяц G3 — $517–642; эталонные прогоны — около $129 (8.5, 12).

---

**Статус**: готово к использованию как рабочий документ фазы 1 — версия 1.7  
**Последнее обновление**: 25 сентября 2026  
**Следующий пересмотр**: на гейте G1, 4 октября 2026
